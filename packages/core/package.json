{
  "name": "@spec/core",
  "version": "0.1.0",
  "description": "Core abstractions of the spec system: SpecNode, Diagnostic, Capability, Spec IR",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "spec": {
    "package": true,
    "entry": "./dist/spec-package.js"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
