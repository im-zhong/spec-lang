{
  "name": "@spec/execution",
  "version": "0.1.0",
  "description": "GitHub-native branch/container/commit/PR execution for compiler-owned agent DAGs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@spec/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
