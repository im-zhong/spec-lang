/**
 * Re-exported from @spec/core — deterministic JSON serialization lives with
 * the core type model so domain packages can use it without depending on
 * the compiler.
 */
export { stableStringify } from "@spec/core"
