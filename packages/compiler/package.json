{
  "name": "@spec/compiler",
  "version": "0.1.0",
  "description": "Static specification compiler: TypeScript AST -> Spec IR with deterministic output",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@spec/core": "workspace:*",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
