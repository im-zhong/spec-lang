{
  "name": "@spec/package-sdk",
  "version": "0.1.0",
  "description": "SDK for authoring specification packages",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@spec/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
