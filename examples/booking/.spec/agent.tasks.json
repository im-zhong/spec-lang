{
  "conformanceFiles": [
    "conformance/behavior_snapshot.py",
    "conformance/conftest.py",
    "conformance/contract.json",
    "conformance/helpers.py",
    "conformance/test_contract.py",
    "conformance/test_infrastructure.py"
  ],
  "dag": {
    "edges": [
      {
        "from": "project",
        "to": "database"
      },
      {
        "from": "project",
        "to": "models"
      },
      {
        "from": "models",
        "to": "schemas"
      },
      {
        "from": "models",
        "to": "security"
      },
      {
        "from": "database",
        "to": "security"
      },
      {
        "from": "models",
        "to": "router:Booking"
      },
      {
        "from": "schemas",
        "to": "router:Booking"
      },
      {
        "from": "database",
        "to": "router:Booking"
      },
      {
        "from": "security",
        "to": "router:Booking"
      },
      {
        "from": "models",
        "to": "router:User"
      },
      {
        "from": "schemas",
        "to": "router:User"
      },
      {
        "from": "database",
        "to": "router:User"
      },
      {
        "from": "security",
        "to": "router:User"
      },
      {
        "from": "models",
        "to": "router:Venue"
      },
      {
        "from": "schemas",
        "to": "router:Venue"
      },
      {
        "from": "database",
        "to": "router:Venue"
      },
      {
        "from": "models",
        "to": "router:auth"
      },
      {
        "from": "schemas",
        "to": "router:auth"
      },
      {
        "from": "database",
        "to": "router:auth"
      },
      {
        "from": "security",
        "to": "router:auth"
      },
      {
        "from": "router:Booking",
        "to": "app"
      },
      {
        "from": "router:User",
        "to": "app"
      },
      {
        "from": "router:Venue",
        "to": "app"
      },
      {
        "from": "router:auth",
        "to": "app"
      },
      {
        "from": "database",
        "to": "app"
      }
    ],
    "tasks": [
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_project.py"
        ],
        "dependsOn": [],
        "id": "project",
        "label": "project skeleton",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: project skeleton\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): pyproject.toml, app/__init__.py, .gitignore\n- Already generated by previous tasks (read-only for you): —\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [file:app-package] The package directory app/ exists with app/__init__.py.\n- [file:gitignore] .gitignore covers bytecode caches and virtualenvs.\n- [pin:pyproject:dependencies] dependencies is EXACTLY the pinned set [bcrypt==5.0.0, email-validator==2.2.0, fastapi==0.141.1, pydantic-settings==2.15.0, pydantic==2.13.5, pyjwt==2.13.0, sqlalchemy==2.0.52, uvicorn==0.52.4] — no additions, removals, or floated versions.\n- [pin:pyproject:dev-dependencies] [project.optional-dependencies] dev is EXACTLY [httpx==0.28.1, pytest==9.1.1].\n- [pin:pyproject:name] pyproject.toml declares project name \"bookingapi\".\n- [pin:pyproject:no-passlib] passlib is never a dependency (it crashes with bcrypt >= 5; bcrypt is already pinned).\n- [pin:pyproject:requires-python] requires-python is exactly \"==3.13.*\".\n- [pin:pyproject:version] pyproject.toml declares version \"0.1.0\".\n\n\n## Forbidden extras\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### App metadata\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```\n\n## Engineering notes\n- pyproject.toml must be installable (hatchling or setuptools).",
            "scope": [
              "pyproject.toml",
              "app/__init__.py",
              ".gitignore"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "file:app-package",
                "kind": "file",
                "level": "api",
                "node": "project",
                "statement": "The package directory app/ exists with app/__init__.py.",
                "verification": "oracle"
              },
              {
                "id": "file:gitignore",
                "kind": "file",
                "level": "api",
                "node": "project",
                "statement": ".gitignore covers bytecode caches and virtualenvs.",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:dependencies",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "dependencies is EXACTLY the pinned set [bcrypt==5.0.0, email-validator==2.2.0, fastapi==0.141.1, pydantic-settings==2.15.0, pydantic==2.13.5, pyjwt==2.13.0, sqlalchemy==2.0.52, uvicorn==0.52.4] — no additions, removals, or floated versions.",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:dev-dependencies",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "[project.optional-dependencies] dev is EXACTLY [httpx==0.28.1, pytest==9.1.1].",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:name",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "pyproject.toml declares project name \"bookingapi\".",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:no-passlib",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "passlib is never a dependency (it crashes with bcrypt >= 5; bcrypt is already pinned).",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:requires-python",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "requires-python is exactly \"==3.13.*\".",
                "verification": "oracle"
              },
              {
                "id": "pin:pyproject:version",
                "kind": "pin",
                "level": "api",
                "node": "project",
                "statement": "pyproject.toml declares version \"0.1.0\".",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_project.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"project\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [file:app-package] The package directory app/ exists with app/__init__.py.\n- [file:gitignore] .gitignore covers bytecode caches and virtualenvs.\n- [pin:pyproject:dependencies] dependencies is EXACTLY the pinned set [bcrypt==5.0.0, email-validator==2.2.0, fastapi==0.141.1, pydantic-settings==2.15.0, pydantic==2.13.5, pyjwt==2.13.0, sqlalchemy==2.0.52, uvicorn==0.52.4] — no additions, removals, or floated versions.\n- [pin:pyproject:dev-dependencies] [project.optional-dependencies] dev is EXACTLY [httpx==0.28.1, pytest==9.1.1].\n- [pin:pyproject:name] pyproject.toml declares project name \"bookingapi\".\n- [pin:pyproject:no-passlib] passlib is never a dependency (it crashes with bcrypt >= 5; bcrypt is already pinned).\n- [pin:pyproject:requires-python] requires-python is exactly \"==3.13.*\".\n- [pin:pyproject:version] pyproject.toml declares version \"0.1.0\".\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_project.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "ed6226c7ac761acf347510c133a2c37d05a7a058050a1110a991a9df4e9952d3",
        "scope": [
          "pyproject.toml",
          "app/__init__.py",
          ".gitignore"
        ],
        "specNodeIds": [
          "app:BookingAPI"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_database.py"
        ],
        "dependsOn": [
          "project"
        ],
        "id": "database",
        "label": "database layer",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: database layer\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/config.py, app/database.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [abi:app:config:exports] app/config.py exports only a pydantic-settings Settings class whose optional database_url field reads DATABASE_URL; it reads no dotenv file and instantiates no settings at module import.\n- [abi:app:database:exports] app/database.py exports exactly Base, normalize_database_url(value), resolve_database_url(explicit=None), create_engine_from_url(explicit=None), create_session_factory(engine), module defaults engine and SessionLocal, get_db(), and session_dependency(factory).\n- [review:app:database:no-extra-apis] (reviewer-judged) app/config.py and app/database.py add no caches, registries, dotenv support, lazy module attributes, or APIs beyond the declared exports.\n\n### function-level clauses\n- [database:get-db] get_db yields from the module-default SessionLocal and always closes the session; session_dependency(factory) returns the same yielding dependency bound to the supplied factory.\n- [database:sessionmaker] create_session_factory(engine) returns a synchronous sessionmaker with autoflush=False and expire_on_commit=False.\n- [database:sqlite-engine] SQLite engines use check_same_thread=False; in-memory SQLite additionally uses StaticPool.\n- [database:url-resolution] URL resolution order is pinned: explicit argument → DATABASE_URL env → \"sqlite:///./dev.db\"; values are ALWAYS SQLAlchemy URLs; a non-empty bare path normalizes to sqlite:///<path>; an empty string uses the fallback.\n\n## Forbidden extras\n- The module's public surface is exactly the declared export list — no additional APIs, registries, or framework code.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Database contract\n```json\n{\n  \"engine\": \"postgres\",\n  \"fallback\": \"sqlite:///./dev.db\",\n  \"urlEnv\": \"DATABASE_URL\",\n  \"urlFormat\": \"sqlalchemy-url\"\n}\n```\n\n## Engineering notes\n- Base is the one SQLAlchemy DeclarativeBase imported by models.\n- Tables are created via Base.metadata.create_all(engine) at app startup (the app task wires this).",
            "scope": [
              "app/config.py",
              "app/database.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "abi:app:config:exports",
                "kind": "abi",
                "level": "api",
                "node": "database",
                "statement": "app/config.py exports only a pydantic-settings Settings class whose optional database_url field reads DATABASE_URL; it reads no dotenv file and instantiates no settings at module import.",
                "verification": "oracle"
              },
              {
                "id": "abi:app:database:exports",
                "kind": "abi",
                "level": "api",
                "node": "database",
                "statement": "app/database.py exports exactly Base, normalize_database_url(value), resolve_database_url(explicit=None), create_engine_from_url(explicit=None), create_session_factory(engine), module defaults engine and SessionLocal, get_db(), and session_dependency(factory).",
                "verification": "oracle"
              },
              {
                "id": "database:get-db",
                "kind": "behavior",
                "level": "function",
                "node": "database",
                "statement": "get_db yields from the module-default SessionLocal and always closes the session; session_dependency(factory) returns the same yielding dependency bound to the supplied factory.",
                "verification": "oracle"
              },
              {
                "id": "database:sessionmaker",
                "kind": "behavior",
                "level": "function",
                "node": "database",
                "statement": "create_session_factory(engine) returns a synchronous sessionmaker with autoflush=False and expire_on_commit=False.",
                "verification": "oracle"
              },
              {
                "id": "database:sqlite-engine",
                "kind": "behavior",
                "level": "function",
                "node": "database",
                "statement": "SQLite engines use check_same_thread=False; in-memory SQLite additionally uses StaticPool.",
                "verification": "oracle"
              },
              {
                "id": "database:url-resolution",
                "kind": "behavior",
                "level": "function",
                "node": "database",
                "statement": "URL resolution order is pinned: explicit argument → DATABASE_URL env → \"sqlite:///./dev.db\"; values are ALWAYS SQLAlchemy URLs; a non-empty bare path normalizes to sqlite:///<path>; an empty string uses the fallback.",
                "verification": "oracle"
              },
              {
                "id": "review:app:database:no-extra-apis",
                "kind": "review",
                "level": "api",
                "node": "database",
                "statement": "app/config.py and app/database.py add no caches, registries, dotenv support, lazy module attributes, or APIs beyond the declared exports.",
                "verification": "review"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_database.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"database\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [abi:app:config:exports] app/config.py exports only a pydantic-settings Settings class whose optional database_url field reads DATABASE_URL; it reads no dotenv file and instantiates no settings at module import.\n- [abi:app:database:exports] app/database.py exports exactly Base, normalize_database_url(value), resolve_database_url(explicit=None), create_engine_from_url(explicit=None), create_session_factory(engine), module defaults engine and SessionLocal, get_db(), and session_dependency(factory).\n- [database:get-db] get_db yields from the module-default SessionLocal and always closes the session; session_dependency(factory) returns the same yielding dependency bound to the supplied factory.\n- [database:sessionmaker] create_session_factory(engine) returns a synchronous sessionmaker with autoflush=False and expire_on_commit=False.\n- [database:sqlite-engine] SQLite engines use check_same_thread=False; in-memory SQLite additionally uses StaticPool.\n- [database:url-resolution] URL resolution order is pinned: explicit argument → DATABASE_URL env → \"sqlite:///./dev.db\"; values are ALWAYS SQLAlchemy URLs; a non-empty bare path normalizes to sqlite:///<path>; an empty string uses the fallback.\n\n## Reviewer-judged clauses (verify by inspection)\n- [review:app:database:no-extra-apis] app/config.py and app/database.py add no caches, registries, dotenv support, lazy module attributes, or APIs beyond the declared exports.\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_database.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "e5f28296bb56c98763bf07557576580aeaacdaaac978374e20cb0a54d182d5e6",
        "scope": [
          "app/config.py",
          "app/database.py"
        ],
        "specNodeIds": [
          "postgres:MainDB"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_models.py"
        ],
        "dependsOn": [
          "project"
        ],
        "id": "models",
        "label": "data models",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: data models\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/models.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [entity:Booking:column:cancelledAt] cancelledAt is a datetime column, nullable\n- [entity:Booking:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:Booking:column:id] id is a uuid column\n- [entity:Booking:column:notes] notes is a string column, nullable\n- [entity:Booking:column:startsAt] startsAt is a datetime column\n- [entity:Booking:column:status] status is a string column validated against {\"pending\", \"confirmed\", \"cancelled\"}, defaulting to the initial state \"pending\"\n- [entity:Booking:column:user] user is a string(36) column with ForeignKey(\"users.id\")\n- [entity:Booking:column:venue] venue is a string(36) column with ForeignKey(\"venues.id\")\n- [entity:Booking:table] Class Booking declares __tablename__ \"bookings\" with the declared column set.\n- [entity:User:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:User:column:email] email is a email column, with a unique constraint\n- [entity:User:column:id] id is a uuid column\n- [entity:User:column:name] name is a string column\n- [entity:User:column:password_hash] password_hash is the implicit bcrypt-hash column on the principal User.\n- [entity:User:table] Class User declares __tablename__ \"users\" with the declared column set.\n- [entity:Venue:column:capacity] capacity is a int column\n- [entity:Venue:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:Venue:column:id] id is a uuid column\n- [entity:Venue:column:name] name is a string column, with a unique constraint\n- [entity:Venue:table] Class Venue declares __tablename__ \"venues\" with the declared column set.\n- [outbox:events:columns] The outbox table events has EXACTLY the columns id (string uuid pk), event (string), payload (string holding a JSON object), created_at (naive-UTC datetime); transitions with emit effects insert into it in-transaction and it exposes no routes.\n\n\n## Forbidden extras\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Entities (the complete data model)\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\",\n    \"table\": \"bookings\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\",\n    \"passwordColumn\": \"password_hash\",\n    \"table\": \"users\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\",\n    \"table\": \"venues\"\n  }\n]\n```\n\n## Engineering notes\n- One class per entity; a shared mixin for id/created_at is idiomatic.\n- The uuid4 default may be Python-side; it must produce distinct ids per insert.",
            "scope": [
              "app/models.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "entity:Booking:column:cancelledAt",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "cancelledAt is a datetime column, nullable",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:created_at",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "created_at is a naive-UTC datetime column set once on insert, present on every entity.",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:id",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "id is a uuid column",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:notes",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "notes is a string column, nullable",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:startsAt",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "startsAt is a datetime column",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:status",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "status is a string column validated against {\"pending\", \"confirmed\", \"cancelled\"}, defaulting to the initial state \"pending\"",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:user",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "user is a string(36) column with ForeignKey(\"users.id\")",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:column:venue",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "venue is a string(36) column with ForeignKey(\"venues.id\")",
                "verification": "oracle"
              },
              {
                "id": "entity:Booking:table",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "Class Booking declares __tablename__ \"bookings\" with the declared column set.",
                "verification": "oracle"
              },
              {
                "id": "entity:User:column:created_at",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "created_at is a naive-UTC datetime column set once on insert, present on every entity.",
                "verification": "oracle"
              },
              {
                "id": "entity:User:column:email",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "email is a email column, with a unique constraint",
                "verification": "oracle"
              },
              {
                "id": "entity:User:column:id",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "id is a uuid column",
                "verification": "oracle"
              },
              {
                "id": "entity:User:column:name",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "name is a string column",
                "verification": "oracle"
              },
              {
                "id": "entity:User:column:password_hash",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "password_hash is the implicit bcrypt-hash column on the principal User.",
                "verification": "oracle"
              },
              {
                "id": "entity:User:table",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "Class User declares __tablename__ \"users\" with the declared column set.",
                "verification": "oracle"
              },
              {
                "id": "entity:Venue:column:capacity",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "capacity is a int column",
                "verification": "oracle"
              },
              {
                "id": "entity:Venue:column:created_at",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "created_at is a naive-UTC datetime column set once on insert, present on every entity.",
                "verification": "oracle"
              },
              {
                "id": "entity:Venue:column:id",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "id is a uuid column",
                "verification": "oracle"
              },
              {
                "id": "entity:Venue:column:name",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "name is a string column, with a unique constraint",
                "verification": "oracle"
              },
              {
                "id": "entity:Venue:table",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "Class Venue declares __tablename__ \"venues\" with the declared column set.",
                "verification": "oracle"
              },
              {
                "id": "outbox:events:columns",
                "kind": "column",
                "level": "api",
                "node": "models",
                "statement": "The outbox table events has EXACTLY the columns id (string uuid pk), event (string), payload (string holding a JSON object), created_at (naive-UTC datetime); transitions with emit effects insert into it in-transaction and it exposes no routes.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_models.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"models\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [entity:Booking:column:cancelledAt] cancelledAt is a datetime column, nullable\n- [entity:Booking:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:Booking:column:id] id is a uuid column\n- [entity:Booking:column:notes] notes is a string column, nullable\n- [entity:Booking:column:startsAt] startsAt is a datetime column\n- [entity:Booking:column:status] status is a string column validated against {\"pending\", \"confirmed\", \"cancelled\"}, defaulting to the initial state \"pending\"\n- [entity:Booking:column:user] user is a string(36) column with ForeignKey(\"users.id\")\n- [entity:Booking:column:venue] venue is a string(36) column with ForeignKey(\"venues.id\")\n- [entity:Booking:table] Class Booking declares __tablename__ \"bookings\" with the declared column set.\n- [entity:User:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:User:column:email] email is a email column, with a unique constraint\n- [entity:User:column:id] id is a uuid column\n- [entity:User:column:name] name is a string column\n- [entity:User:column:password_hash] password_hash is the implicit bcrypt-hash column on the principal User.\n- [entity:User:table] Class User declares __tablename__ \"users\" with the declared column set.\n- [entity:Venue:column:capacity] capacity is a int column\n- [entity:Venue:column:created_at] created_at is a naive-UTC datetime column set once on insert, present on every entity.\n- [entity:Venue:column:id] id is a uuid column\n- [entity:Venue:column:name] name is a string column, with a unique constraint\n- [entity:Venue:table] Class Venue declares __tablename__ \"venues\" with the declared column set.\n- [outbox:events:columns] The outbox table events has EXACTLY the columns id (string uuid pk), event (string), payload (string holding a JSON object), created_at (naive-UTC datetime); transitions with emit effects insert into it in-transaction and it exposes no routes.\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_models.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "fc6e87a3541911e95a42ca16e88ef7339a2ee78f8875b78c693380eef09612c6",
        "scope": [
          "app/models.py"
        ],
        "specNodeIds": [
          "entity:Booking",
          "entity:User",
          "entity:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_schemas.py"
        ],
        "dependsOn": [
          "models"
        ],
        "id": "schemas",
        "label": "pydantic schemas",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: pydantic schemas\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/schemas.py\n- Already generated by previous tasks (read-only for you): app/models.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [schemas:Booking:create] The BookingCreate model accepts exactly {cancelledAt, id, notes, startsAt, user, venue} (defaulted and optional fields omittable), excluding the state field status.\n- [schemas:Booking:response] The BookingOut model emits EXACTLY {cancelledAt, id, notes, startsAt, status, user, venue} (never password_hash/created_at).\n- [schemas:Booking:update] The BookingUpdate model makes every accepted field optional — partial PATCH semantics, excluding the state field status.\n- [schemas:User:create] The UserCreate model accepts exactly {email, id, name} (defaulted and optional fields omittable).\n- [schemas:User:response] The UserOut model emits EXACTLY {email, id, name} (never password_hash/created_at).\n- [schemas:User:update] The UserUpdate model makes every accepted field optional — partial PATCH semantics.\n- [schemas:Venue:create] The VenueCreate model accepts exactly {capacity, id, name} (defaulted and optional fields omittable).\n- [schemas:Venue:response] The VenueOut model emits EXACTLY {capacity, id, name} (never password_hash/created_at).\n- [schemas:Venue:update] The VenueUpdate model makes every accepted field optional — partial PATCH semantics.\n\n### function-level clauses\n- [schemas:Booking:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n- [schemas:User:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n- [schemas:Venue:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n\n## Forbidden extras\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Entities\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\"\n  }\n]\n```\n\n## Engineering notes\n- Response serialization is contract-critical; the clause table pins the exact key sets.",
            "scope": [
              "app/schemas.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "contract:serialization:implicit-columns-hidden",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "password_hash and created_at are never serialized; created_at only orders lists.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:keys",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "Response keys are the declared field names EXACTLY (camelCase stays camelCase).",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:refs-as-ids",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "ref fields serialize as the referenced row's id string.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:uuid4-ids",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The server generates uuid4 ids; id in request bodies is ignored.",
                "verification": "oracle"
              },
              {
                "id": "schemas:Booking:create",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The BookingCreate model accepts exactly {cancelledAt, id, notes, startsAt, user, venue} (defaulted and optional fields omittable), excluding the state field status.",
                "verification": "oracle"
              },
              {
                "id": "schemas:Booking:response",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The BookingOut model emits EXACTLY {cancelledAt, id, notes, startsAt, status, user, venue} (never password_hash/created_at).",
                "verification": "oracle"
              },
              {
                "id": "schemas:Booking:update",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The BookingUpdate model makes every accepted field optional — partial PATCH semantics, excluding the state field status.",
                "verification": "oracle"
              },
              {
                "id": "schemas:Booking:validation",
                "kind": "serialization",
                "level": "function",
                "node": "schemas",
                "statement": "Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.",
                "verification": "oracle"
              },
              {
                "id": "schemas:User:create",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The UserCreate model accepts exactly {email, id, name} (defaulted and optional fields omittable).",
                "verification": "oracle"
              },
              {
                "id": "schemas:User:response",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The UserOut model emits EXACTLY {email, id, name} (never password_hash/created_at).",
                "verification": "oracle"
              },
              {
                "id": "schemas:User:update",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The UserUpdate model makes every accepted field optional — partial PATCH semantics.",
                "verification": "oracle"
              },
              {
                "id": "schemas:User:validation",
                "kind": "serialization",
                "level": "function",
                "node": "schemas",
                "statement": "Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.",
                "verification": "oracle"
              },
              {
                "id": "schemas:Venue:create",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The VenueCreate model accepts exactly {capacity, id, name} (defaulted and optional fields omittable).",
                "verification": "oracle"
              },
              {
                "id": "schemas:Venue:response",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The VenueOut model emits EXACTLY {capacity, id, name} (never password_hash/created_at).",
                "verification": "oracle"
              },
              {
                "id": "schemas:Venue:update",
                "kind": "serialization",
                "level": "api",
                "node": "schemas",
                "statement": "The VenueUpdate model makes every accepted field optional — partial PATCH semantics.",
                "verification": "oracle"
              },
              {
                "id": "schemas:Venue:validation",
                "kind": "serialization",
                "level": "function",
                "node": "schemas",
                "statement": "Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_schemas.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"schemas\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [schemas:Booking:create] The BookingCreate model accepts exactly {cancelledAt, id, notes, startsAt, user, venue} (defaulted and optional fields omittable), excluding the state field status.\n- [schemas:Booking:response] The BookingOut model emits EXACTLY {cancelledAt, id, notes, startsAt, status, user, venue} (never password_hash/created_at).\n- [schemas:Booking:update] The BookingUpdate model makes every accepted field optional — partial PATCH semantics, excluding the state field status.\n- [schemas:Booking:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n- [schemas:User:create] The UserCreate model accepts exactly {email, id, name} (defaulted and optional fields omittable).\n- [schemas:User:response] The UserOut model emits EXACTLY {email, id, name} (never password_hash/created_at).\n- [schemas:User:update] The UserUpdate model makes every accepted field optional — partial PATCH semantics.\n- [schemas:User:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n- [schemas:Venue:create] The VenueCreate model accepts exactly {capacity, id, name} (defaulted and optional fields omittable).\n- [schemas:Venue:response] The VenueOut model emits EXACTLY {capacity, id, name} (never password_hash/created_at).\n- [schemas:Venue:update] The VenueUpdate model makes every accepted field optional — partial PATCH semantics.\n- [schemas:Venue:validation] Field validation follows declared types: string/int/boolean/uuid/email/datetime; ref fields are id strings; enum fields validate against their states.\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_schemas.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "35adf2edd7fe284f9e313ec42181028b32f21376e7cda88c1b4c794f9dcfc2ea",
        "scope": [
          "app/schemas.py"
        ],
        "specNodeIds": [
          "entity:Booking",
          "entity:User",
          "entity:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_security.py"
        ],
        "dependsOn": [
          "models",
          "database"
        ],
        "id": "security",
        "label": "auth security",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: auth security\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/security.py, app/deps.py\n- Already generated by previous tasks (read-only for you): app/models.py, app/database.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [abi:app:security:exports] app/security.py exports hash_password, verify_password(password, password_hash | None) -> bool, create_access_token(subject) -> str, and decode_access_token(token) -> str | None.\n\n### function-level clauses\n- [review:security:uuid-parse] (reviewer-judged) When models use SQLAlchemy Uuid columns, get_current_user parses uuid.UUID(sub) before db.get(...) — raw string binds against Uuid columns raise and 500.\n- [security:bcrypt-direct] Password hashing uses the bcrypt package DIRECTLY (bcrypt.hashpw / bcrypt.checkpw), never passlib, and truncates secrets to 72 bytes in both hash and verify.\n- [security:deps-401] get_current_user in app/deps.py raises 401 {\"detail\":\"Not authenticated\"} (exact body) for a missing or invalid token, using HTTPBearer(auto_error=False) — the default auto_error raises 403, which violates the contract.\n- [security:token-roundtrip] create_access_token(subject) followed by decode_access_token returns the subject; decode_access_token of garbage returns None; tokens carry sub = principal id with secret/expiry via env.\n- [security:verify-none] verify_password(password, None) — and any ValueError — returns False without raising.\n\n## Forbidden extras\n- The module's public surface is exactly the declared export list — no additional APIs, registries, or framework code.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Auth contract\n```json\n{\n  \"auth\": {\n    \"identityField\": \"email\",\n    \"passwordColumn\": \"password_hash\",\n    \"principal\": \"User\",\n    \"routes\": [\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/login\",\n        \"method\": \"POST\",\n        \"operation\": \"login\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/login\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"kind\": \"token\"\n        },\n        \"status\": 200\n      },\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/register\",\n        \"method\": \"POST\",\n        \"operation\": \"register\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/register\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"email\",\n            \"name\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 201\n      },\n      {\n        \"auth\": true,\n        \"entity\": \"User\",\n        \"id\": \"GET /auth/me\",\n        \"method\": \"GET\",\n        \"operation\": \"me\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/me\",\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 200\n      }\n    ],\n    \"strategy\": \"password-jwt\"\n  },\n  \"unauthenticated\": {\n    \"body\": {\n      \"detail\": \"Not authenticated\"\n    },\n    \"status\": 401\n  }\n}\n```\n\n## Engineering notes\n- JWT bearer tokens carry sub = principal id with secret/expiry via env.",
            "scope": [
              "app/security.py",
              "app/deps.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "abi:app:security:exports",
                "kind": "abi",
                "level": "api",
                "node": "security",
                "statement": "app/security.py exports hash_password, verify_password(password, password_hash | None) -> bool, create_access_token(subject) -> str, and decode_access_token(token) -> str | None.",
                "verification": "oracle"
              },
              {
                "id": "review:security:uuid-parse",
                "kind": "review",
                "level": "function",
                "node": "security",
                "statement": "When models use SQLAlchemy Uuid columns, get_current_user parses uuid.UUID(sub) before db.get(...) — raw string binds against Uuid columns raise and 500.",
                "verification": "review"
              },
              {
                "id": "security:bcrypt-direct",
                "kind": "adapter",
                "level": "function",
                "node": "security",
                "statement": "Password hashing uses the bcrypt package DIRECTLY (bcrypt.hashpw / bcrypt.checkpw), never passlib, and truncates secrets to 72 bytes in both hash and verify.",
                "verification": "oracle"
              },
              {
                "id": "security:deps-401",
                "kind": "behavior",
                "level": "function",
                "node": "security",
                "statement": "get_current_user in app/deps.py raises 401 {\"detail\":\"Not authenticated\"} (exact body) for a missing or invalid token, using HTTPBearer(auto_error=False) — the default auto_error raises 403, which violates the contract.",
                "verification": "oracle"
              },
              {
                "id": "security:token-roundtrip",
                "kind": "behavior",
                "level": "function",
                "node": "security",
                "statement": "create_access_token(subject) followed by decode_access_token returns the subject; decode_access_token of garbage returns None; tokens carry sub = principal id with secret/expiry via env.",
                "verification": "oracle"
              },
              {
                "id": "security:verify-none",
                "kind": "behavior",
                "level": "function",
                "node": "security",
                "statement": "verify_password(password, None) — and any ValueError — returns False without raising.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_security.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"security\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [abi:app:security:exports] app/security.py exports hash_password, verify_password(password, password_hash | None) -> bool, create_access_token(subject) -> str, and decode_access_token(token) -> str | None.\n- [security:bcrypt-direct] Password hashing uses the bcrypt package DIRECTLY (bcrypt.hashpw / bcrypt.checkpw), never passlib, and truncates secrets to 72 bytes in both hash and verify.\n- [security:deps-401] get_current_user in app/deps.py raises 401 {\"detail\":\"Not authenticated\"} (exact body) for a missing or invalid token, using HTTPBearer(auto_error=False) — the default auto_error raises 403, which violates the contract.\n- [security:token-roundtrip] create_access_token(subject) followed by decode_access_token returns the subject; decode_access_token of garbage returns None; tokens carry sub = principal id with secret/expiry via env.\n- [security:verify-none] verify_password(password, None) — and any ValueError — returns False without raising.\n\n## Reviewer-judged clauses (verify by inspection)\n- [review:security:uuid-parse] When models use SQLAlchemy Uuid columns, get_current_user parses uuid.UUID(sub) before db.get(...) — raw string binds against Uuid columns raise and 500.\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_security.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "bb24ec86338b71cd55ec83b649793364edeb652e0e35424af10fc2d2f7372246",
        "scope": [
          "app/security.py",
          "app/deps.py"
        ],
        "specNodeIds": [
          "auth:MainAuth"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_booking.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:Booking",
        "label": "router: Booking",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Booking\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/booking.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [contract:serialization:create-defaults] Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:Booking:count-before-id] The count route is registered BEFORE any {id} route of the same prefix.\n- [import:router:Booking:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:Booking:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [invariant:no-overbooking] no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)\n- [route:DELETE /bookings/{id}] Route DELETE /bookings/{id} exists (bearer auth required, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /bookings] Route GET /bookings exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /bookings/{id}] Route GET /bookings/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /bookings/count] Route GET /bookings/count exists (bearer auth required, success status 200); count returns 200 with the exact body {\"count\": <int>} (total rows).\n- [route:POST /bookings] Route POST /bookings exists (bearer auth required, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.\n- [route:POST /bookings/{id}/cancel:transition] POST /bookings/{id}/cancel atomically transitions status from {confirmed, pending} to \"cancelled\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; effects in transaction order: [{\"__effect\":\"set\",\"field\":\"cancelledAt\",\"value\":{\"__expr\":\"requestTime\"}}]\n- [route:POST /bookings/{id}/confirm:transition] POST /bookings/{id}/confirm atomically transitions status from {pending} to \"confirmed\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; guard expression: {\"__expr\":\"cmp\",\"left\":{\"__expr\":\"field\",\"name\":\"startsAt\"},\"op\":\"gt\",\"right\":{\"__expr\":\"requestTime\"}}; effects in transaction order: [{\"__effect\":\"emit\",\"event\":\"booking.confirmed\",\"fields\":[\"id\",\"venue\",\"startsAt\"]}]\n\n\n## Forbidden extras\n- No routes exist beyond the clause-listed interface of this task.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"cancelled_at\",\n      \"name\": \"cancelledAt\",\n      \"optional\": true,\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"notes\",\n      \"name\": \"notes\",\n      \"optional\": true,\n      \"type\": \"string\"\n    },\n    {\n      \"column\": \"starts_at\",\n      \"name\": \"startsAt\",\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"status\",\n      \"name\": \"status\",\n      \"states\": [\n        \"pending\",\n        \"confirmed\",\n        \"cancelled\"\n      ],\n      \"type\": \"enum\"\n    },\n    {\n      \"column\": \"user\",\n      \"name\": \"user\",\n      \"target\": \"User\",\n      \"type\": \"ref\"\n    },\n    {\n      \"column\": \"venue\",\n      \"name\": \"venue\",\n      \"target\": \"Venue\",\n      \"type\": \"ref\"\n    }\n  ],\n  \"name\": \"Booking\",\n  \"table\": \"bookings\"\n}\n```\n### Guard expressions (data, from the specification)\n```json\n[\n  {\n    \"event\": \"confirm\",\n    \"guard\": {\n      \"__expr\": \"cmp\",\n      \"left\": {\n        \"__expr\": \"field\",\n        \"name\": \"startsAt\"\n      },\n      \"op\": \"gt\",\n      \"right\": {\n        \"__expr\": \"requestTime\"\n      }\n    }\n  }\n]\n```\n### Effects (data, applied inside the transition transaction in declared order)\n```json\n[\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"emit\",\n        \"event\": \"booking.confirmed\",\n        \"fields\": [\n          \"id\",\n          \"venue\",\n          \"startsAt\"\n        ]\n      }\n    ],\n    \"event\": \"confirm\"\n  },\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"set\",\n        \"field\": \"cancelledAt\",\n        \"value\": {\n          \"__expr\": \"requestTime\"\n        }\n      }\n    ],\n    \"event\": \"cancel\"\n  }\n]\n```\n### Invariant check trees (data, from the specification)\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Engineering notes\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Guards evaluate `requestTime` (the request's receipt time, naive UTC) ONCE per request, bound into the SQL comparison — never baked into code.\n- If a reference-validation helper needs a model-class type, use `type[Any]` (with `Any` from `typing`) or omit that annotation.",
            "scope": [
              "app/routers/booking.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "contract:serialization:create-defaults",
                "kind": "serialization",
                "level": "api",
                "node": "router:Booking",
                "statement": "Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:implicit-columns-hidden",
                "kind": "serialization",
                "level": "api",
                "node": "router:Booking",
                "statement": "password_hash and created_at are never serialized; created_at only orders lists.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:keys",
                "kind": "serialization",
                "level": "api",
                "node": "router:Booking",
                "statement": "Response keys are the declared field names EXACTLY (camelCase stays camelCase).",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:refs-as-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:Booking",
                "statement": "ref fields serialize as the referenced row's id string.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:uuid4-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:Booking",
                "statement": "The server generates uuid4 ids; id in request bodies is ignored.",
                "verification": "oracle"
              },
              {
                "id": "import:router:Booking:count-before-id",
                "kind": "import",
                "level": "api",
                "node": "router:Booking",
                "statement": "The count route is registered BEFORE any {id} route of the same prefix.",
                "verification": "oracle"
              },
              {
                "id": "import:router:Booking:no-orm-base",
                "kind": "import",
                "level": "api",
                "node": "router:Booking",
                "statement": "The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.",
                "verification": "oracle"
              },
              {
                "id": "import:router:Booking:sqlalchemy-locations",
                "kind": "import",
                "level": "api",
                "node": "router:Booking",
                "statement": "SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.",
                "verification": "oracle"
              },
              {
                "id": "invariant:no-overbooking",
                "kind": "invariant",
                "level": "api",
                "node": "router:Booking",
                "statement": "no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)",
                "verification": "oracle"
              },
              {
                "id": "route:DELETE /bookings/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:Booking",
                "statement": "Route DELETE /bookings/{id} exists (bearer auth required, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /bookings",
                "kind": "route",
                "level": "api",
                "node": "router:Booking",
                "statement": "Route GET /bookings exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /bookings/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:Booking",
                "statement": "Route GET /bookings/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /bookings/count",
                "kind": "route",
                "level": "api",
                "node": "router:Booking",
                "statement": "Route GET /bookings/count exists (bearer auth required, success status 200); count returns 200 with the exact body {\"count\": <int>} (total rows).",
                "verification": "oracle"
              },
              {
                "id": "route:POST /bookings",
                "kind": "route",
                "level": "api",
                "node": "router:Booking",
                "statement": "Route POST /bookings exists (bearer auth required, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.",
                "verification": "oracle"
              },
              {
                "id": "route:POST /bookings/{id}/cancel:transition",
                "kind": "transition",
                "level": "api",
                "node": "router:Booking",
                "statement": "POST /bookings/{id}/cancel atomically transitions status from {confirmed, pending} to \"cancelled\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; effects in transaction order: [{\"__effect\":\"set\",\"field\":\"cancelledAt\",\"value\":{\"__expr\":\"requestTime\"}}]",
                "verification": "oracle"
              },
              {
                "id": "route:POST /bookings/{id}/confirm:transition",
                "kind": "transition",
                "level": "api",
                "node": "router:Booking",
                "statement": "POST /bookings/{id}/confirm atomically transitions status from {pending} to \"confirmed\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; guard expression: {\"__expr\":\"cmp\",\"left\":{\"__expr\":\"field\",\"name\":\"startsAt\"},\"op\":\"gt\",\"right\":{\"__expr\":\"requestTime\"}}; effects in transaction order: [{\"__effect\":\"emit\",\"event\":\"booking.confirmed\",\"fields\":[\"id\",\"venue\",\"startsAt\"]}]",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_booking.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"router:Booking\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [contract:serialization:create-defaults] Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:Booking:count-before-id] The count route is registered BEFORE any {id} route of the same prefix.\n- [import:router:Booking:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:Booking:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [invariant:no-overbooking] no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)\n- [route:DELETE /bookings/{id}] Route DELETE /bookings/{id} exists (bearer auth required, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /bookings] Route GET /bookings exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /bookings/{id}] Route GET /bookings/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /bookings/count] Route GET /bookings/count exists (bearer auth required, success status 200); count returns 200 with the exact body {\"count\": <int>} (total rows).\n- [route:POST /bookings] Route POST /bookings exists (bearer auth required, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.\n- [route:POST /bookings/{id}/cancel:transition] POST /bookings/{id}/cancel atomically transitions status from {confirmed, pending} to \"cancelled\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; effects in transaction order: [{\"__effect\":\"set\",\"field\":\"cancelledAt\",\"value\":{\"__expr\":\"requestTime\"}}]\n- [route:POST /bookings/{id}/confirm:transition] POST /bookings/{id}/confirm atomically transitions status from {pending} to \"confirmed\"; a wrong current state or failed guard answers 409 {\"detail\":\"Invalid state\"}; an unknown id answers 404 {\"detail\":\"Not found\"}; success returns 200 with the updated row; guard expression: {\"__expr\":\"cmp\",\"left\":{\"__expr\":\"field\",\"name\":\"startsAt\"},\"op\":\"gt\",\"right\":{\"__expr\":\"requestTime\"}}; effects in transaction order: [{\"__effect\":\"emit\",\"event\":\"booking.confirmed\",\"fields\":[\"id\",\"venue\",\"startsAt\"]}]\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_router_booking.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "917a0469453475c62866745cec01e69489893dd71e75f8d26da392baf379de99",
        "scope": [
          "app/routers/booking.py"
        ],
        "specNodeIds": [
          "api:BookingCount",
          "crud:Booking",
          "lifecycle:BookingLifecycle"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_user.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:User",
        "label": "router: User",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: User\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/user.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:User:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:User:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [route:GET /users] Route GET /users exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /users/{id}] Route GET /users/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n\n\n## Forbidden extras\n- No routes exist beyond the clause-listed interface of this task.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```\n\n## Engineering notes\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Guards evaluate `requestTime` (the request's receipt time, naive UTC) ONCE per request, bound into the SQL comparison — never baked into code.\n- If a reference-validation helper needs a model-class type, use `type[Any]` (with `Any` from `typing`) or omit that annotation.",
            "scope": [
              "app/routers/user.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "contract:serialization:implicit-columns-hidden",
                "kind": "serialization",
                "level": "api",
                "node": "router:User",
                "statement": "password_hash and created_at are never serialized; created_at only orders lists.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:keys",
                "kind": "serialization",
                "level": "api",
                "node": "router:User",
                "statement": "Response keys are the declared field names EXACTLY (camelCase stays camelCase).",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:refs-as-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:User",
                "statement": "ref fields serialize as the referenced row's id string.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:uuid4-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:User",
                "statement": "The server generates uuid4 ids; id in request bodies is ignored.",
                "verification": "oracle"
              },
              {
                "id": "import:router:User:no-orm-base",
                "kind": "import",
                "level": "api",
                "node": "router:User",
                "statement": "The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.",
                "verification": "oracle"
              },
              {
                "id": "import:router:User:sqlalchemy-locations",
                "kind": "import",
                "level": "api",
                "node": "router:User",
                "statement": "SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /users",
                "kind": "route",
                "level": "api",
                "node": "router:User",
                "statement": "Route GET /users exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /users/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:User",
                "statement": "Route GET /users/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_user.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"router:User\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:User:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:User:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [route:GET /users] Route GET /users exists (bearer auth required, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /users/{id}] Route GET /users/{id} exists (bearer auth required, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_router_user.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "3b373104022a69fa5debe49dea2d1526b20f278fa083187ce2db1ba9cde019fb",
        "scope": [
          "app/routers/user.py"
        ],
        "specNodeIds": [
          "crud:User"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_venue.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database"
        ],
        "id": "router:Venue",
        "label": "router: Venue",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Venue\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/venue.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/models.py, app/schemas.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [contract:serialization:create-defaults] Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:Venue:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:Venue:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [invariant:no-overbooking] no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)\n- [route:DELETE /venues/{id}] Route DELETE /venues/{id} exists (public, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /venues] Route GET /venues exists (public, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /venues/{id}] Route GET /venues/{id} exists (public, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:PATCH /venues/{id}] Route PATCH /venues/{id} exists (public, success status 200); update is a partial PATCH returning 200 with the full row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:PATCH /venues/{id}:error:alreadyExists] A unique violation answers 409 {\"detail\":\"Already exists\"}.\n- [route:POST /venues] Route POST /venues exists (public, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.\n- [route:POST /venues:error:alreadyExists] A unique violation answers 409 {\"detail\":\"Already exists\"}.\n\n\n## Forbidden extras\n- No routes exist beyond the clause-listed interface of this task.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"capacity\",\n      \"name\": \"capacity\",\n      \"type\": \"int\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\",\n      \"unique\": true\n    }\n  ],\n  \"name\": \"Venue\",\n  \"table\": \"venues\"\n}\n```\n### Invariant check trees (data, from the specification)\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Engineering notes\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Guards evaluate `requestTime` (the request's receipt time, naive UTC) ONCE per request, bound into the SQL comparison — never baked into code.\n- If a reference-validation helper needs a model-class type, use `type[Any]` (with `Any` from `typing`) or omit that annotation.",
            "scope": [
              "app/routers/venue.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "contract:serialization:create-defaults",
                "kind": "serialization",
                "level": "api",
                "node": "router:Venue",
                "statement": "Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:implicit-columns-hidden",
                "kind": "serialization",
                "level": "api",
                "node": "router:Venue",
                "statement": "password_hash and created_at are never serialized; created_at only orders lists.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:keys",
                "kind": "serialization",
                "level": "api",
                "node": "router:Venue",
                "statement": "Response keys are the declared field names EXACTLY (camelCase stays camelCase).",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:refs-as-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:Venue",
                "statement": "ref fields serialize as the referenced row's id string.",
                "verification": "oracle"
              },
              {
                "id": "contract:serialization:uuid4-ids",
                "kind": "serialization",
                "level": "api",
                "node": "router:Venue",
                "statement": "The server generates uuid4 ids; id in request bodies is ignored.",
                "verification": "oracle"
              },
              {
                "id": "import:router:Venue:no-orm-base",
                "kind": "import",
                "level": "api",
                "node": "router:Venue",
                "statement": "The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.",
                "verification": "oracle"
              },
              {
                "id": "import:router:Venue:sqlalchemy-locations",
                "kind": "import",
                "level": "api",
                "node": "router:Venue",
                "statement": "SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.",
                "verification": "oracle"
              },
              {
                "id": "invariant:no-overbooking",
                "kind": "invariant",
                "level": "api",
                "node": "router:Venue",
                "statement": "no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)",
                "verification": "oracle"
              },
              {
                "id": "route:DELETE /venues/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:Venue",
                "statement": "Route DELETE /venues/{id} exists (public, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /venues",
                "kind": "route",
                "level": "api",
                "node": "router:Venue",
                "statement": "Route GET /venues exists (public, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /venues/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:Venue",
                "statement": "Route GET /venues/{id} exists (public, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              },
              {
                "id": "route:PATCH /venues/{id}",
                "kind": "route",
                "level": "api",
                "node": "router:Venue",
                "statement": "Route PATCH /venues/{id} exists (public, success status 200); update is a partial PATCH returning 200 with the full row, or 404 {\"detail\":\"Not found\"} for an unknown id.",
                "verification": "oracle"
              },
              {
                "id": "route:PATCH /venues/{id}:error:alreadyExists",
                "kind": "error",
                "level": "api",
                "node": "router:Venue",
                "statement": "A unique violation answers 409 {\"detail\":\"Already exists\"}.",
                "verification": "oracle"
              },
              {
                "id": "route:POST /venues",
                "kind": "route",
                "level": "api",
                "node": "router:Venue",
                "statement": "Route POST /venues exists (public, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.",
                "verification": "oracle"
              },
              {
                "id": "route:POST /venues:error:alreadyExists",
                "kind": "error",
                "level": "api",
                "node": "router:Venue",
                "statement": "A unique violation answers 409 {\"detail\":\"Already exists\"}.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_venue.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"router:Venue\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [contract:serialization:create-defaults] Fields with a declared default are omittable in create bodies (the default applies when omitted); optional-without-default stores null.\n- [contract:serialization:implicit-columns-hidden] password_hash and created_at are never serialized; created_at only orders lists.\n- [contract:serialization:keys] Response keys are the declared field names EXACTLY (camelCase stays camelCase).\n- [contract:serialization:refs-as-ids] ref fields serialize as the referenced row's id string.\n- [contract:serialization:uuid4-ids] The server generates uuid4 ids; id in request bodies is ignored.\n- [import:router:Venue:no-orm-base] The router defines no ORM base and imports neither Base nor DeclarativeBase; all mapped classes come from app.models.\n- [import:router:Venue:sqlalchemy-locations] SQLAlchemy import locations are exact: func, select, and update from sqlalchemy; IntegrityError from sqlalchemy.exc; Session from sqlalchemy.orm; never DeclarativeBase from top-level sqlalchemy.\n- [invariant:no-overbooking] no-overbooking: for every Venue row, the count of Booking rows whose venue points at it must be ≤ <row>.capacity, enforced inside the request transaction on create/update of Booking and update of Venue; violations answer 409 {\"detail\":\"Invariant violated\"} (row rolled back)\n- [route:DELETE /venues/{id}] Route DELETE /venues/{id} exists (public, success status 204); delete returns 204 with an empty body, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:GET /venues] Route GET /venues exists (public, success status 200); list returns 200 with EVERY row as a bare JSON array ordered by created_at ascending.\n- [route:GET /venues/{id}] Route GET /venues/{id} exists (public, success status 200); get returns 200 with the row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:PATCH /venues/{id}] Route PATCH /venues/{id} exists (public, success status 200); update is a partial PATCH returning 200 with the full row, or 404 {\"detail\":\"Not found\"} for an unknown id.\n- [route:PATCH /venues/{id}:error:alreadyExists] A unique violation answers 409 {\"detail\":\"Already exists\"}.\n- [route:POST /venues] Route POST /venues exists (public, success status 201); create returns 201 with the stored row and maps dangling ref ids to 404 {\"detail\":\"Not found\"}.\n- [route:POST /venues:error:alreadyExists] A unique violation answers 409 {\"detail\":\"Already exists\"}.\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_router_venue.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "708334d18636d893648a5ed56847a76c694c453a4ce45739d965e49fd3ccd5c4",
        "scope": [
          "app/routers/venue.py"
        ],
        "specNodeIds": [
          "crud:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_auth.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:auth",
        "label": "router: auth",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: auth\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/auth.py\n- Already generated by previous tasks (read-only for you): app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [contract:serialization:never-password-hash] password_hash is never serialized.\n- [route:GET /auth/me] GET /auth/me with a bearer token returns 200 with the principal row.\n- [route:POST /auth/login] POST /auth/login accepts {\"email\": ..., \"password\": ...} and returns 200 {\"access_token\": \"<jwt>\", \"token_type\": \"bearer\"}; a wrong identity or password answers 401 {\"detail\":\"Invalid credentials\"} (identical for both — no user enumeration).\n- [route:POST /auth/register] POST /auth/register accepts the principal fields plus \"password\" (bcrypt-hashed before storage) and returns 201 with the principal row (never the hash); a duplicate identity answers 409 {\"detail\":\"Already exists\"}.\n\n\n## Forbidden extras\n- No routes exist beyond the clause-listed interface of this task.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Auth contract\n```json\n{\n  \"identityField\": \"email\",\n  \"passwordColumn\": \"password_hash\",\n  \"principal\": \"User\",\n  \"routes\": [\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/login\",\n      \"method\": \"POST\",\n      \"operation\": \"login\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/login\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"kind\": \"token\"\n      },\n      \"status\": 200\n    },\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/register\",\n      \"method\": \"POST\",\n      \"operation\": \"register\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/register\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"email\",\n          \"name\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 201\n    },\n    {\n      \"auth\": true,\n      \"entity\": \"User\",\n      \"id\": \"GET /auth/me\",\n      \"method\": \"GET\",\n      \"operation\": \"me\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/me\",\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 200\n    }\n  ],\n  \"strategy\": \"password-jwt\"\n}\n```\n### Principal entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```",
            "scope": [
              "app/routers/auth.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "contract:serialization:never-password-hash",
                "kind": "serialization",
                "level": "api",
                "node": "router:auth",
                "statement": "password_hash is never serialized.",
                "verification": "oracle"
              },
              {
                "id": "route:GET /auth/me",
                "kind": "route",
                "level": "api",
                "node": "router:auth",
                "statement": "GET /auth/me with a bearer token returns 200 with the principal row.",
                "verification": "oracle"
              },
              {
                "id": "route:POST /auth/login",
                "kind": "route",
                "level": "api",
                "node": "router:auth",
                "statement": "POST /auth/login accepts {\"email\": ..., \"password\": ...} and returns 200 {\"access_token\": \"<jwt>\", \"token_type\": \"bearer\"}; a wrong identity or password answers 401 {\"detail\":\"Invalid credentials\"} (identical for both — no user enumeration).",
                "verification": "oracle"
              },
              {
                "id": "route:POST /auth/register",
                "kind": "route",
                "level": "api",
                "node": "router:auth",
                "statement": "POST /auth/register accepts the principal fields plus \"password\" (bcrypt-hashed before storage) and returns 201 with the principal row (never the hash); a duplicate identity answers 409 {\"detail\":\"Already exists\"}.",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_router_auth.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"router:auth\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [contract:serialization:never-password-hash] password_hash is never serialized.\n- [route:GET /auth/me] GET /auth/me with a bearer token returns 200 with the principal row.\n- [route:POST /auth/login] POST /auth/login accepts {\"email\": ..., \"password\": ...} and returns 200 {\"access_token\": \"<jwt>\", \"token_type\": \"bearer\"}; a wrong identity or password answers 401 {\"detail\":\"Invalid credentials\"} (identical for both — no user enumeration).\n- [route:POST /auth/register] POST /auth/register accepts the principal fields plus \"password\" (bcrypt-hashed before storage) and returns 201 with the principal row (never the hash); a duplicate identity answers 409 {\"detail\":\"Already exists\"}.\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_router_auth.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "b9f28f2921ed69c3adb3d7900dda09933207c871dcd0b5bc9c6bed1b364ee00e",
        "scope": [
          "app/routers/auth.py"
        ],
        "specNodeIds": [
          "auth:MainAuth"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_app.py"
        ],
        "dependsOn": [
          "router:Booking",
          "router:User",
          "router:Venue",
          "router:auth",
          "database"
        ],
        "id": "app",
        "label": "application wiring",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: application wiring\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/main.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/routers/auth.py, app/routers/booking.py, app/routers/user.py, app/routers/venue.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n## Node contract (clause table)\n\nThese clauses are the COMPLETE behavioral contract for this task. Each is\nmachine-verified (oracle) or reviewer-judged (review) exactly as written;\nanything not stated here is implementation freedom.\n\n### api-level clauses\n- [abi:app:main:exports] app/main.py exports create_app(database_url: str | None = None) -> FastAPI AND a module-level app = create_app().\n- [app:router-registry] ROUTERS is imported from the compiler-owned app.router_registry and each entry is included exactly once in tuple order; routers are imported/registered by no other path.\n- [app:routes-complete] The application exposes EXACTLY the declared route interface {GET /bookings; GET /bookings/{id}; POST /bookings; DELETE /bookings/{id}; GET /users; GET /users/{id}; GET /venues; GET /venues/{id}; POST /venues; PATCH /venues/{id}; DELETE /venues/{id}; GET /bookings/count; POST /bookings/{id}/confirm; POST /bookings/{id}/cancel; POST /auth/login; POST /auth/register; GET /auth/me} — strict OpenAPI equality (FastAPI's automatic /openapi.json and /docs are fine).\n- [app:title-version] The application title is \"Booking API\" and the version \"0.1.0\".\n\n### function-level clauses\n- [app:engine-isolation] Each create_app call creates one engine via create_engine_from_url(database_url) and one session factory, stores them on app.state, overrides get_db with session_dependency(factory), creates tables on startup against that engine, and disposes it on shutdown — this is what makes separate create_app(database_url=...) calls isolated.\n- [app:state-adapters] Deterministic in-memory cache, messaging, and blob adapters are constructed by default and exposed as app.state.cache, app.state.messaging, and app.state.blob when their corresponding contracts exist.\n\n## Forbidden extras\n- The module's public surface is exactly the declared export list — no additional APIs, registries, or framework code.\n- Files beyond your scope are never created or modified.\n\n## Shared operational constraints\n- Path parameters are named exactly `id`.\n- Error bodies are exact JSON, never wrapped, renamed, or extended: 401 {\"detail\":\"Not authenticated\"} · 404 {\"detail\":\"Not found\"} · 409 bodies as pinned per clause · 422 = the FastAPI default.\n- SQLite-compatible SQL only (tests run on SQLite).\n- Do not add any route, file, or path beyond your scope — the conformance suite asserts STRICT OpenAPI equality.\n\n## Contract challenge protocol\nIf you conclude this contract is internally unsatisfiable or wrong, make no\nedits and reply with exactly one JSON object and nothing else:\n{\"challenge\":{\"clause\":\"<clause id>\",\"reason\":\"<one paragraph>\"}}\nNever improvise around a defect; challenging it is the only correct response.\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the node contract in the clause table.\nIf guidance appears to conflict with a clause, implement the clause.\n\n## Reference data (subordinate to the clause table)\n### Application\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```\n\n## Engineering notes\n- Count routes must be reachable after inclusion (registration order comes from the compiler-owned registry tuple).",
            "scope": [
              "app/main.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "clauses": [
              {
                "id": "abi:app:main:exports",
                "kind": "abi",
                "level": "api",
                "node": "app",
                "statement": "app/main.py exports create_app(database_url: str | None = None) -> FastAPI AND a module-level app = create_app().",
                "verification": "oracle"
              },
              {
                "id": "app:engine-isolation",
                "kind": "app",
                "level": "function",
                "node": "app",
                "statement": "Each create_app call creates one engine via create_engine_from_url(database_url) and one session factory, stores them on app.state, overrides get_db with session_dependency(factory), creates tables on startup against that engine, and disposes it on shutdown — this is what makes separate create_app(database_url=...) calls isolated.",
                "verification": "oracle"
              },
              {
                "id": "app:router-registry",
                "kind": "app",
                "level": "api",
                "node": "app",
                "statement": "ROUTERS is imported from the compiler-owned app.router_registry and each entry is included exactly once in tuple order; routers are imported/registered by no other path.",
                "verification": "oracle"
              },
              {
                "id": "app:routes-complete",
                "kind": "app",
                "level": "api",
                "node": "app",
                "statement": "The application exposes EXACTLY the declared route interface {GET /bookings; GET /bookings/{id}; POST /bookings; DELETE /bookings/{id}; GET /users; GET /users/{id}; GET /venues; GET /venues/{id}; POST /venues; PATCH /venues/{id}; DELETE /venues/{id}; GET /bookings/count; POST /bookings/{id}/confirm; POST /bookings/{id}/cancel; POST /auth/login; POST /auth/register; GET /auth/me} — strict OpenAPI equality (FastAPI's automatic /openapi.json and /docs are fine).",
                "verification": "oracle"
              },
              {
                "id": "app:state-adapters",
                "kind": "app",
                "level": "function",
                "node": "app",
                "statement": "Deterministic in-memory cache, messaging, and blob adapters are constructed by default and exposed as app.state.cache, app.state.messaging, and app.state.blob when their corresponding contracts exist.",
                "verification": "oracle"
              },
              {
                "id": "app:title-version",
                "kind": "app",
                "level": "api",
                "node": "app",
                "statement": "The application title is \"Booking API\" and the version \"0.1.0\".",
                "verification": "oracle"
              }
            ],
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_oracle/test_app.py"
            ],
            "instruction": "You are the read-only reviewer for generation node \"app\".\nVerify the implementation against the frozen node contract below. The\noracle clauses are already machine-verified by the compiler-owned tests in\nthe evidence — confirm the implementation does not merely game those\ntests (hardcoded outputs, condition-special-casing, dead code paths).\nThe reviewer-judged clauses MUST be verified by code inspection.\n\n## Oracle clauses (machine-verified — check for gaming, not re-derivation)\n- [abi:app:main:exports] app/main.py exports create_app(database_url: str | None = None) -> FastAPI AND a module-level app = create_app().\n- [app:engine-isolation] Each create_app call creates one engine via create_engine_from_url(database_url) and one session factory, stores them on app.state, overrides get_db with session_dependency(factory), creates tables on startup against that engine, and disposes it on shutdown — this is what makes separate create_app(database_url=...) calls isolated.\n- [app:router-registry] ROUTERS is imported from the compiler-owned app.router_registry and each entry is included exactly once in tuple order; routers are imported/registered by no other path.\n- [app:routes-complete] The application exposes EXACTLY the declared route interface {GET /bookings; GET /bookings/{id}; POST /bookings; DELETE /bookings/{id}; GET /users; GET /users/{id}; GET /venues; GET /venues/{id}; POST /venues; PATCH /venues/{id}; DELETE /venues/{id}; GET /bookings/count; POST /bookings/{id}/confirm; POST /bookings/{id}/cancel; POST /auth/login; POST /auth/register; GET /auth/me} — strict OpenAPI equality (FastAPI's automatic /openapi.json and /docs are fine).\n- [app:state-adapters] Deterministic in-memory cache, messaging, and blob adapters are constructed by default and exposed as app.state.cache, app.state.messaging, and app.state.blob when their corresponding contracts exist.\n- [app:title-version] The application title is \"Booking API\" and the version \"0.1.0\".\n\n## Reviewer-judged clauses (verify by inspection)\n- (none)\n\nLook for missing behavior, extra public API or routes, ABI drift, invalid\nimports, and uncovered constraints. Do not edit any file. Your result must\nbe exactly one JSON object and nothing else: {\"approved\":boolean,\"feedback\":\"specific changes keyed to clause ids\"}.",
            "oracleFiles": [
              "tests/spec_oracle/runner.py",
              "tests/spec_oracle/test_app.py"
            ]
          },
          "schemaVersion": "spec-agent-task-loop/0.2"
        },
        "promptSha256": "2533ee7bd4abe311916c9156395104764af1066b9ab85543320c254019581b56",
        "scope": [
          "app/main.py"
        ],
        "specNodeIds": [
          "app:BookingAPI",
          "fastapi:Server"
        ]
      }
    ]
  },
  "verification": {
    "check": [
      {
        "command": ".venv/bin/python -c \"from app.main import app, create_app; assert app.title\"",
        "name": "import",
        "timeoutMs": 60000
      },
      {
        "command": ".venv/bin/python -m pytest conformance -q",
        "name": "conformance",
        "timeoutMs": 300000
      }
    ],
    "setup": [
      {
        "command": "uv venv .venv --clear --quiet --python 3.13",
        "name": "venv",
        "timeoutMs": 180000
      },
      {
        "command": "uv pip install --quiet -e '.[dev]'",
        "name": "install",
        "timeoutMs": 600000
      }
    ]
  }
}
