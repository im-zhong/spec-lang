{
  "conformanceFiles": [
    "conformance/behavior_snapshot.py",
    "conformance/conftest.py",
    "conformance/contract.json",
    "conformance/helpers.py",
    "conformance/test_contract.py",
    "conformance/test_infrastructure.py"
  ],
  "dag": {
    "edges": [
      {
        "from": "project",
        "to": "database"
      },
      {
        "from": "project",
        "to": "models"
      },
      {
        "from": "models",
        "to": "schemas"
      },
      {
        "from": "models",
        "to": "security"
      },
      {
        "from": "database",
        "to": "security"
      },
      {
        "from": "models",
        "to": "router:Booking"
      },
      {
        "from": "schemas",
        "to": "router:Booking"
      },
      {
        "from": "database",
        "to": "router:Booking"
      },
      {
        "from": "security",
        "to": "router:Booking"
      },
      {
        "from": "models",
        "to": "router:User"
      },
      {
        "from": "schemas",
        "to": "router:User"
      },
      {
        "from": "database",
        "to": "router:User"
      },
      {
        "from": "security",
        "to": "router:User"
      },
      {
        "from": "models",
        "to": "router:Venue"
      },
      {
        "from": "schemas",
        "to": "router:Venue"
      },
      {
        "from": "database",
        "to": "router:Venue"
      },
      {
        "from": "models",
        "to": "router:auth"
      },
      {
        "from": "schemas",
        "to": "router:auth"
      },
      {
        "from": "database",
        "to": "router:auth"
      },
      {
        "from": "security",
        "to": "router:auth"
      },
      {
        "from": "router:Booking",
        "to": "app"
      },
      {
        "from": "router:User",
        "to": "app"
      },
      {
        "from": "router:Venue",
        "to": "app"
      },
      {
        "from": "router:auth",
        "to": "app"
      },
      {
        "from": "database",
        "to": "app"
      }
    ],
    "tasks": [
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_project.py"
        ],
        "dependsOn": [],
        "id": "project",
        "label": "project skeleton",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: project skeleton\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): pyproject.toml, app/__init__.py, .gitignore\n- Already generated by previous tasks (read-only for you): —\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nCreate the Python project skeleton for \"Booking API\".\n\n## Requirements\n- `pyproject.toml`: project name `bookingapi`, version\n  `0.1.0`, installable (hatchling or setuptools),\n  `requires-python = \"==3.13.*\"`, with EXACTLY these pinned\n  dependencies (the stack is part of the specification — do not add,\n  remove or float any version):\n  ```toml\n  dependencies = [\n    \"bcrypt==5.0.0\",\n    \"email-validator==2.2.0\",\n    \"fastapi==0.141.1\",\n    \"pydantic==2.13.5\",\n    \"pydantic-settings==2.15.0\",\n    \"pyjwt==2.13.0\",\n    \"sqlalchemy==2.0.52\",\n    \"uvicorn==0.52.4\",\n  ]\n\n  [project.optional-dependencies]\n  dev = [\n    \"httpx==0.28.1\",\n    \"pytest==9.1.1\",\n  ]\n  ```\n  Do NOT add passlib (it crashes with bcrypt >= 5); bcrypt is already\n  pinned.\n- Package directory `app/` with `app/__init__.py`.\n- A `.gitignore` covering caches and virtualenvs.\n\n## App metadata\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```",
            "scope": [
              "pyproject.toml",
              "app/__init__.py",
              ".gitignore"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_project.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"project\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"project\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_project.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_project.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: project skeleton\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): pyproject.toml, app/__init__.py, .gitignore\n- Already generated by previous tasks (read-only for you): —\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nCreate the Python project skeleton for \"Booking API\".\n\n## Requirements\n- `pyproject.toml`: project name `bookingapi`, version\n  `0.1.0`, installable (hatchling or setuptools),\n  `requires-python = \"==3.13.*\"`, with EXACTLY these pinned\n  dependencies (the stack is part of the specification — do not add,\n  remove or float any version):\n  ```toml\n  dependencies = [\n    \"bcrypt==5.0.0\",\n    \"email-validator==2.2.0\",\n    \"fastapi==0.141.1\",\n    \"pydantic==2.13.5\",\n    \"pydantic-settings==2.15.0\",\n    \"pyjwt==2.13.0\",\n    \"sqlalchemy==2.0.52\",\n    \"uvicorn==0.52.4\",\n  ]\n\n  [project.optional-dependencies]\n  dev = [\n    \"httpx==0.28.1\",\n    \"pytest==9.1.1\",\n  ]\n  ```\n  Do NOT add passlib (it crashes with bcrypt >= 5); bcrypt is already\n  pinned.\n- Package directory `app/` with `app/__init__.py`.\n- A `.gitignore` covering caches and virtualenvs.\n\n## App metadata\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```",
            "scope": [
              "tests/spec_tasks/test_project.py"
            ]
          }
        },
        "promptSha256": "6fe644c8950c28a769706ff54ad26399d034bbc17ffe0d756887c0f6d2af4f93",
        "scope": [
          ".gitignore",
          "app/__init__.py",
          "pyproject.toml",
          "tests/spec_tasks/test_project.py"
        ],
        "specNodeIds": [
          "app:BookingAPI"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_database.py"
        ],
        "dependsOn": [
          "project"
        ],
        "id": "database",
        "label": "database layer",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: database layer\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/config.py, app/database.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the database/engine layer.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Database contract\n```json\n{\n  \"engine\": \"postgres\",\n  \"fallback\": \"sqlite:///./dev.db\",\n  \"urlEnv\": \"DATABASE_URL\",\n  \"urlFormat\": \"sqlalchemy-url\"\n}\n```\n\n## Requirements\n- `app/config.py` exports only a pydantic-settings `Settings` class whose\n  optional `database_url` field reads `DATABASE_URL`; it does not read a\n  dotenv file or instantiate settings at module import.\n- `app/database.py` exports `Base`, `normalize_database_url(value)`,\n  `resolve_database_url(explicit=None)`, `create_engine_from_url(explicit=None)`,\n  `create_session_factory(engine)`, module defaults `engine` and\n  `SessionLocal`, `get_db()`, and `session_dependency(factory)`.\n- `Base` is the one SQLAlchemy `DeclarativeBase` imported by models.\n  `create_session_factory` returns a synchronous sessionmaker with\n  `autoflush=False` and `expire_on_commit=False`. SQLite engines use\n  `check_same_thread=False`; in-memory SQLite additionally uses `StaticPool`.\n- `get_db` yields from the module default `SessionLocal` and always closes\n  the session. `session_dependency(factory)` returns the same yielding\n  dependency bound to the supplied factory, for `create_app` isolation.\n- URL resolution order (pinned): explicit argument → `DATABASE_URL` env →\n  `sqlite:///./dev.db`. Values are ALWAYS SQLAlchemy URLs\n  (`sqlite:///...`, `postgresql+psycopg://...`); a non-empty bare path is\n  normalized to `sqlite:///<path>`, and an empty string uses the fallback.\n- Tables are created via `Base.metadata.create_all(engine)` at app\n  startup (the app task wires this). Do not add caches, registries, dotenv\n  support, lazy module attributes, or other APIs.",
            "scope": [
              "app/config.py",
              "app/database.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_database.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"database\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"database\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_database.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_database.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: database layer\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/config.py, app/database.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the database/engine layer.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Database contract\n```json\n{\n  \"engine\": \"postgres\",\n  \"fallback\": \"sqlite:///./dev.db\",\n  \"urlEnv\": \"DATABASE_URL\",\n  \"urlFormat\": \"sqlalchemy-url\"\n}\n```\n\n## Requirements\n- `app/config.py` exports only a pydantic-settings `Settings` class whose\n  optional `database_url` field reads `DATABASE_URL`; it does not read a\n  dotenv file or instantiate settings at module import.\n- `app/database.py` exports `Base`, `normalize_database_url(value)`,\n  `resolve_database_url(explicit=None)`, `create_engine_from_url(explicit=None)`,\n  `create_session_factory(engine)`, module defaults `engine` and\n  `SessionLocal`, `get_db()`, and `session_dependency(factory)`.\n- `Base` is the one SQLAlchemy `DeclarativeBase` imported by models.\n  `create_session_factory` returns a synchronous sessionmaker with\n  `autoflush=False` and `expire_on_commit=False`. SQLite engines use\n  `check_same_thread=False`; in-memory SQLite additionally uses `StaticPool`.\n- `get_db` yields from the module default `SessionLocal` and always closes\n  the session. `session_dependency(factory)` returns the same yielding\n  dependency bound to the supplied factory, for `create_app` isolation.\n- URL resolution order (pinned): explicit argument → `DATABASE_URL` env →\n  `sqlite:///./dev.db`. Values are ALWAYS SQLAlchemy URLs\n  (`sqlite:///...`, `postgresql+psycopg://...`); a non-empty bare path is\n  normalized to `sqlite:///<path>`, and an empty string uses the fallback.\n- Tables are created via `Base.metadata.create_all(engine)` at app\n  startup (the app task wires this). Do not add caches, registries, dotenv\n  support, lazy module attributes, or other APIs.",
            "scope": [
              "tests/spec_tasks/test_database.py"
            ]
          }
        },
        "promptSha256": "7068ca08f026c9f096d275afeb45ed58dce321bcc2b10253977f2bbf92889295",
        "scope": [
          "app/config.py",
          "app/database.py",
          "tests/spec_tasks/test_database.py"
        ],
        "specNodeIds": [
          "postgres:MainDB"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_models.py"
        ],
        "dependsOn": [
          "project"
        ],
        "id": "models",
        "label": "data models",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: data models\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/models.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the SQLAlchemy models (`app/models.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entities (the complete data model)\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\",\n    \"table\": \"bookings\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\",\n    \"passwordColumn\": \"password_hash\",\n    \"table\": \"users\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\",\n    \"table\": \"venues\"\n  }\n]\n```\n\n## Requirements\n- One class per entity; `__tablename__` = the blueprint's `table`\n  (snake_case plural); columns = the blueprint's `column` names.\n- `id`: string uuid primary key, server-side uuid4 default (Python-side\n  default is fine).\n- `created_at`: naive-UTC datetime column on EVERY entity, set once on\n  insert (a shared mixin is idiomatic).\n- The principal `User` additionally gets the implicit column `password_hash` (bcrypt hash).\n- `ref` fields become string(36) columns with ForeignKey(\"<target table>.id\").\n- `enum` fields become string columns validated against their `states`;\n  a lifecycle-bound enum column defaults to the lifecycle's initial state.\n- Unique constraints for `unique` fields; nullable for `optional` fields.\n- ALSO generate the outbox table `events` with EXACTLY\n  these columns: id (string uuid pk), event (string), payload (string\n  holding a JSON object), created_at (naive-UTC datetime). Transitions\n  with emit effects insert into it in-transaction; expose NO routes\n  for it.",
            "scope": [
              "app/models.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_models.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"models\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"models\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_models.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_models.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: data models\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/models.py\n- Already generated by previous tasks (read-only for you): app/__init__.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the SQLAlchemy models (`app/models.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entities (the complete data model)\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\",\n    \"table\": \"bookings\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\",\n    \"passwordColumn\": \"password_hash\",\n    \"table\": \"users\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\",\n    \"table\": \"venues\"\n  }\n]\n```\n\n## Requirements\n- One class per entity; `__tablename__` = the blueprint's `table`\n  (snake_case plural); columns = the blueprint's `column` names.\n- `id`: string uuid primary key, server-side uuid4 default (Python-side\n  default is fine).\n- `created_at`: naive-UTC datetime column on EVERY entity, set once on\n  insert (a shared mixin is idiomatic).\n- The principal `User` additionally gets the implicit column `password_hash` (bcrypt hash).\n- `ref` fields become string(36) columns with ForeignKey(\"<target table>.id\").\n- `enum` fields become string columns validated against their `states`;\n  a lifecycle-bound enum column defaults to the lifecycle's initial state.\n- Unique constraints for `unique` fields; nullable for `optional` fields.\n- ALSO generate the outbox table `events` with EXACTLY\n  these columns: id (string uuid pk), event (string), payload (string\n  holding a JSON object), created_at (naive-UTC datetime). Transitions\n  with emit effects insert into it in-transaction; expose NO routes\n  for it.",
            "scope": [
              "tests/spec_tasks/test_models.py"
            ]
          }
        },
        "promptSha256": "08133ff1cddea5d8c0f742f14184b59d4678a73c495e41ff9e7979bd27e37978",
        "scope": [
          "app/models.py",
          "tests/spec_tasks/test_models.py"
        ],
        "specNodeIds": [
          "entity:Booking",
          "entity:User",
          "entity:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_schemas.py"
        ],
        "dependsOn": [
          "models"
        ],
        "id": "schemas",
        "label": "pydantic schemas",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: pydantic schemas\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/schemas.py\n- Already generated by previous tasks (read-only for you): app/models.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the request/response schemas (`app/schemas.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entities\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\"\n  }\n]\n```\n\n## Requirements\n- Per entity: a Create model (all fields except `id`; defaulted and\n  optional fields omittable), an Update model (every field optional —\n  partial PATCH semantics), and a response serializer producing EXACTLY\n  the declared field names in responses (never password_hash/created_at).\n- Field validation by type: string/int/boolean/uuid/email/datetime; ref\n  fields are id strings; enum fields validate against their `states`.\n- Lifecycle-bound state fields are EXCLUDED from Create and Update models\n  entirely (the server assigns the initial state; PATCH ignores the field).\n- Response serialization is contract-critical: keys and casing must match\n  the declared field names exactly.",
            "scope": [
              "app/schemas.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_schemas.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"schemas\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"schemas\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_schemas.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_schemas.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: pydantic schemas\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/schemas.py\n- Already generated by previous tasks (read-only for you): app/models.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the request/response schemas (`app/schemas.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entities\n```json\n[\n  {\n    \"fields\": [\n      {\n        \"column\": \"cancelled_at\",\n        \"name\": \"cancelledAt\",\n        \"optional\": true,\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"notes\",\n        \"name\": \"notes\",\n        \"optional\": true,\n        \"type\": \"string\"\n      },\n      {\n        \"column\": \"starts_at\",\n        \"name\": \"startsAt\",\n        \"type\": \"datetime\"\n      },\n      {\n        \"column\": \"status\",\n        \"name\": \"status\",\n        \"states\": [\n          \"pending\",\n          \"confirmed\",\n          \"cancelled\"\n        ],\n        \"type\": \"enum\"\n      },\n      {\n        \"column\": \"user\",\n        \"name\": \"user\",\n        \"target\": \"User\",\n        \"type\": \"ref\"\n      },\n      {\n        \"column\": \"venue\",\n        \"name\": \"venue\",\n        \"target\": \"Venue\",\n        \"type\": \"ref\"\n      }\n    ],\n    \"name\": \"Booking\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"email\",\n        \"name\": \"email\",\n        \"type\": \"email\",\n        \"unique\": true\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\"\n      }\n    ],\n    \"name\": \"User\"\n  },\n  {\n    \"fields\": [\n      {\n        \"column\": \"capacity\",\n        \"name\": \"capacity\",\n        \"type\": \"int\"\n      },\n      {\n        \"column\": \"id\",\n        \"name\": \"id\",\n        \"type\": \"uuid\"\n      },\n      {\n        \"column\": \"name\",\n        \"name\": \"name\",\n        \"type\": \"string\",\n        \"unique\": true\n      }\n    ],\n    \"name\": \"Venue\"\n  }\n]\n```\n\n## Requirements\n- Per entity: a Create model (all fields except `id`; defaulted and\n  optional fields omittable), an Update model (every field optional —\n  partial PATCH semantics), and a response serializer producing EXACTLY\n  the declared field names in responses (never password_hash/created_at).\n- Field validation by type: string/int/boolean/uuid/email/datetime; ref\n  fields are id strings; enum fields validate against their `states`.\n- Lifecycle-bound state fields are EXCLUDED from Create and Update models\n  entirely (the server assigns the initial state; PATCH ignores the field).\n- Response serialization is contract-critical: keys and casing must match\n  the declared field names exactly.",
            "scope": [
              "tests/spec_tasks/test_schemas.py"
            ]
          }
        },
        "promptSha256": "49977c3f8f98c179cf9268b879b71b3e40ab4c3960113631093ddcf8ea6428f9",
        "scope": [
          "app/schemas.py",
          "tests/spec_tasks/test_schemas.py"
        ],
        "specNodeIds": [
          "entity:Booking",
          "entity:User",
          "entity:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_security.py"
        ],
        "dependsOn": [
          "models",
          "database"
        ],
        "id": "security",
        "label": "auth security",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: auth security\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/security.py, app/deps.py\n- Already generated by previous tasks (read-only for you): app/models.py, app/database.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement JWT/bcrypt security (`app/security.py`, `app/deps.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Auth contract\n```json\n{\n  \"auth\": {\n    \"identityField\": \"email\",\n    \"passwordColumn\": \"password_hash\",\n    \"principal\": \"User\",\n    \"routes\": [\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/login\",\n        \"method\": \"POST\",\n        \"operation\": \"login\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/login\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"kind\": \"token\"\n        },\n        \"status\": 200\n      },\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/register\",\n        \"method\": \"POST\",\n        \"operation\": \"register\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/register\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"email\",\n            \"name\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 201\n      },\n      {\n        \"auth\": true,\n        \"entity\": \"User\",\n        \"id\": \"GET /auth/me\",\n        \"method\": \"GET\",\n        \"operation\": \"me\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/me\",\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 200\n      }\n    ],\n    \"strategy\": \"password-jwt\"\n  },\n  \"unauthenticated\": {\n    \"body\": {\n      \"detail\": \"Not authenticated\"\n    },\n    \"status\": 401\n  }\n}\n```\n\n## Requirements\n- Password hashing with the `bcrypt` package DIRECTLY (`bcrypt.hashpw` /\n  `bcrypt.checkpw`) — do NOT use passlib (passlib 1.7.4 crashes with\n  bcrypt >= 5). bcrypt rejects secrets longer than 72 bytes: truncate to\n  72 bytes in both hash and verify.\n- Expose `hash_password`, `verify_password(password, password_hash | None) -> bool`\n  (None or ValueError → False), `create_access_token(subject) -> str`,\n  `decode_access_token(token) -> str | None` from `app/security.py`.\n- JWT bearer tokens (sub = principal id, secret/expiry via env).\n- `get_current_user` dependency in `app/deps.py`: missing/invalid token →\n  401 `{\"detail\": \"Not authenticated\"}` (exact body). Use\n  `HTTPBearer(auto_error=False)` — the default `auto_error` raises **403**,\n  which violates the contract. If the models use SQLAlchemy `Uuid` columns,\n  parse `uuid.UUID(sub)` before `db.get(...)` — raw string binds against\n  `Uuid` columns raise and 500.",
            "scope": [
              "app/security.py",
              "app/deps.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_security.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"security\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"security\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_security.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_security.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: auth security\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/security.py, app/deps.py\n- Already generated by previous tasks (read-only for you): app/models.py, app/database.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement JWT/bcrypt security (`app/security.py`, `app/deps.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Auth contract\n```json\n{\n  \"auth\": {\n    \"identityField\": \"email\",\n    \"passwordColumn\": \"password_hash\",\n    \"principal\": \"User\",\n    \"routes\": [\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/login\",\n        \"method\": \"POST\",\n        \"operation\": \"login\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/login\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"kind\": \"token\"\n        },\n        \"status\": 200\n      },\n      {\n        \"auth\": false,\n        \"entity\": \"User\",\n        \"id\": \"POST /auth/register\",\n        \"method\": \"POST\",\n        \"operation\": \"register\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/register\",\n        \"request\": {\n          \"shape\": {\n            \"email\": \"email\",\n            \"name\": \"string\",\n            \"password\": \"string\"\n          }\n        },\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 201\n      },\n      {\n        \"auth\": true,\n        \"entity\": \"User\",\n        \"id\": \"GET /auth/me\",\n        \"method\": \"GET\",\n        \"operation\": \"me\",\n        \"owner\": {\n          \"kind\": \"auth\",\n          \"sourceNodeId\": \"auth:MainAuth\",\n          \"taskId\": \"router:auth\"\n        },\n        \"path\": \"/auth/me\",\n        \"response\": {\n          \"entity\": \"User\",\n          \"kind\": \"entity\"\n        },\n        \"status\": 200\n      }\n    ],\n    \"strategy\": \"password-jwt\"\n  },\n  \"unauthenticated\": {\n    \"body\": {\n      \"detail\": \"Not authenticated\"\n    },\n    \"status\": 401\n  }\n}\n```\n\n## Requirements\n- Password hashing with the `bcrypt` package DIRECTLY (`bcrypt.hashpw` /\n  `bcrypt.checkpw`) — do NOT use passlib (passlib 1.7.4 crashes with\n  bcrypt >= 5). bcrypt rejects secrets longer than 72 bytes: truncate to\n  72 bytes in both hash and verify.\n- Expose `hash_password`, `verify_password(password, password_hash | None) -> bool`\n  (None or ValueError → False), `create_access_token(subject) -> str`,\n  `decode_access_token(token) -> str | None` from `app/security.py`.\n- JWT bearer tokens (sub = principal id, secret/expiry via env).\n- `get_current_user` dependency in `app/deps.py`: missing/invalid token →\n  401 `{\"detail\": \"Not authenticated\"}` (exact body). Use\n  `HTTPBearer(auto_error=False)` — the default `auto_error` raises **403**,\n  which violates the contract. If the models use SQLAlchemy `Uuid` columns,\n  parse `uuid.UUID(sub)` before `db.get(...)` — raw string binds against\n  `Uuid` columns raise and 500.",
            "scope": [
              "tests/spec_tasks/test_security.py"
            ]
          }
        },
        "promptSha256": "6f125f5c2ecca1904345bac4c9bee456f798b5af52f847b4737a524b9a05f0ae",
        "scope": [
          "app/deps.py",
          "app/security.py",
          "tests/spec_tasks/test_security.py"
        ],
        "specNodeIds": [
          "auth:MainAuth"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_booking.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:Booking",
        "label": "router: Booking",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Booking\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/booking.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **Booking** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"cancelled_at\",\n      \"name\": \"cancelledAt\",\n      \"optional\": true,\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"notes\",\n      \"name\": \"notes\",\n      \"optional\": true,\n      \"type\": \"string\"\n    },\n    {\n      \"column\": \"starts_at\",\n      \"name\": \"startsAt\",\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"status\",\n      \"name\": \"status\",\n      \"states\": [\n        \"pending\",\n        \"confirmed\",\n        \"cancelled\"\n      ],\n      \"type\": \"enum\"\n    },\n    {\n      \"column\": \"user\",\n      \"name\": \"user\",\n      \"target\": \"User\",\n      \"type\": \"ref\"\n    },\n    {\n      \"column\": \"venue\",\n      \"name\": \"venue\",\n      \"target\": \"Venue\",\n      \"type\": \"ref\"\n    }\n  ],\n  \"name\": \"Booking\",\n  \"table\": \"bookings\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /bookings | 200 | bearer | list |\n| GET | /bookings/{id} | 200 | bearer | get |\n| POST | /bookings | 201 | bearer | create |\n| DELETE | /bookings/{id} | 204 | bearer | delete |\n| GET | /bookings/count | 200 | bearer | count |\n| POST | /bookings/{id}/confirm | 200 | bearer | transition |\n| POST | /bookings/{id}/cancel | 200 | bearer | transition |\n\n### Lifecycle transitions (state machine — the \"when it is allowed\" contract)\n\n| event | route | guard (current state) | sets |\n| --- | --- | --- | --- |\n| confirm | POST /bookings/{id}/confirm | status ∈ {pending} + guard | status = confirmed + effects |\n| cancel | POST /bookings/{id}/cancel | status ∈ {confirmed, pending} | status = cancelled + effects |\n\nImplement each transition as an ATOMIC guarded update — the guard never\nleaves SQL, so two concurrent transitions of the same row serialize and\nthe loser observes the guard failure:\n\n```sql\nUPDATE <table> SET <state_col> = '<to>'\nWHERE id = :id AND <state_col> IN ('<from>', ...) RETURNING *;\n```\n\nPinned outcomes (exact bodies):\n- success → 200 with the updated row\n- row exists but wrong current state (or a failed guard) → 409 `{\"detail\": \"Invalid state\"}`\n- unknown id → 404 `{\"detail\": \"Not found\"}`\n\nThe state field is server-controlled: it must NOT appear in create or\nupdate schemas (create assigns the initial state; update ignores it).\n\nGuards (beyond the state check) are expression trees over row fields,\nconstants and `requestTime` (the request's receipt time, naive UTC —\nevaluate it ONCE per request and bind it into the SQL comparison;\nnever bake a timestamp into code). Guard data:\n\n```json\n[\n  {\n    \"event\": \"confirm\",\n    \"guard\": {\n      \"__expr\": \"cmp\",\n      \"left\": {\n        \"__expr\": \"field\",\n        \"name\": \"startsAt\"\n      },\n      \"op\": \"gt\",\n      \"right\": {\n        \"__expr\": \"requestTime\"\n      }\n    }\n  }\n]\n```\n\nEffects run inside the transition's transaction, in declared order,\nall-or-nothing:\n- `set`: assign the field in the same UPDATE (value: a constant or\n  requestTime, naive UTC).\n- `emit`: INSERT a row into the generated outbox table `events` —\n  columns EXACTLY (id TEXT uuid pk, event TEXT, payload TEXT holding a\n  JSON object, created_at datetime). The payload object's keys are the\n  effect's declared field names with the post-transition row's values.\n\nEffects data:\n\n```json\n[\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"emit\",\n        \"event\": \"booking.confirmed\",\n        \"fields\": [\n          \"id\",\n          \"venue\",\n          \"startsAt\"\n        ]\n      }\n    ],\n    \"event\": \"confirm\"\n  },\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"set\",\n        \"field\": \"cancelledAt\",\n        \"value\": {\n          \"__expr\": \"requestTime\"\n        }\n      }\n    ],\n    \"event\": \"cancel\"\n  }\n]\n```\n\n### Invariants (truths that must hold at ALL times)\n\n- no-overbooking: for every Venue row — count of Booking rows whose\n  venue points at it must be ≤ <row>.capacity.\n  Enforced in create/update handlers of Booking and update handlers of\n  Venue, INSIDE the request transaction: apply the mutation, re-check\n  the affected Venue row, roll back and answer 409 on violation.\n\nPinned violation outcome (exact body, row rolled back):\n409 `{\"detail\": \"Invariant violated\"}`\n\nCheck trees (data, from the specification):\n\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Protected routes require the bearer token dependency (`get_current_user`).",
            "scope": [
              "app/routers/booking.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_booking.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"router:Booking\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"router:Booking\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_router_booking.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_router_booking.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Booking\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/booking.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **Booking** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"cancelled_at\",\n      \"name\": \"cancelledAt\",\n      \"optional\": true,\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"notes\",\n      \"name\": \"notes\",\n      \"optional\": true,\n      \"type\": \"string\"\n    },\n    {\n      \"column\": \"starts_at\",\n      \"name\": \"startsAt\",\n      \"type\": \"datetime\"\n    },\n    {\n      \"column\": \"status\",\n      \"name\": \"status\",\n      \"states\": [\n        \"pending\",\n        \"confirmed\",\n        \"cancelled\"\n      ],\n      \"type\": \"enum\"\n    },\n    {\n      \"column\": \"user\",\n      \"name\": \"user\",\n      \"target\": \"User\",\n      \"type\": \"ref\"\n    },\n    {\n      \"column\": \"venue\",\n      \"name\": \"venue\",\n      \"target\": \"Venue\",\n      \"type\": \"ref\"\n    }\n  ],\n  \"name\": \"Booking\",\n  \"table\": \"bookings\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /bookings | 200 | bearer | list |\n| GET | /bookings/{id} | 200 | bearer | get |\n| POST | /bookings | 201 | bearer | create |\n| DELETE | /bookings/{id} | 204 | bearer | delete |\n| GET | /bookings/count | 200 | bearer | count |\n| POST | /bookings/{id}/confirm | 200 | bearer | transition |\n| POST | /bookings/{id}/cancel | 200 | bearer | transition |\n\n### Lifecycle transitions (state machine — the \"when it is allowed\" contract)\n\n| event | route | guard (current state) | sets |\n| --- | --- | --- | --- |\n| confirm | POST /bookings/{id}/confirm | status ∈ {pending} + guard | status = confirmed + effects |\n| cancel | POST /bookings/{id}/cancel | status ∈ {confirmed, pending} | status = cancelled + effects |\n\nImplement each transition as an ATOMIC guarded update — the guard never\nleaves SQL, so two concurrent transitions of the same row serialize and\nthe loser observes the guard failure:\n\n```sql\nUPDATE <table> SET <state_col> = '<to>'\nWHERE id = :id AND <state_col> IN ('<from>', ...) RETURNING *;\n```\n\nPinned outcomes (exact bodies):\n- success → 200 with the updated row\n- row exists but wrong current state (or a failed guard) → 409 `{\"detail\": \"Invalid state\"}`\n- unknown id → 404 `{\"detail\": \"Not found\"}`\n\nThe state field is server-controlled: it must NOT appear in create or\nupdate schemas (create assigns the initial state; update ignores it).\n\nGuards (beyond the state check) are expression trees over row fields,\nconstants and `requestTime` (the request's receipt time, naive UTC —\nevaluate it ONCE per request and bind it into the SQL comparison;\nnever bake a timestamp into code). Guard data:\n\n```json\n[\n  {\n    \"event\": \"confirm\",\n    \"guard\": {\n      \"__expr\": \"cmp\",\n      \"left\": {\n        \"__expr\": \"field\",\n        \"name\": \"startsAt\"\n      },\n      \"op\": \"gt\",\n      \"right\": {\n        \"__expr\": \"requestTime\"\n      }\n    }\n  }\n]\n```\n\nEffects run inside the transition's transaction, in declared order,\nall-or-nothing:\n- `set`: assign the field in the same UPDATE (value: a constant or\n  requestTime, naive UTC).\n- `emit`: INSERT a row into the generated outbox table `events` —\n  columns EXACTLY (id TEXT uuid pk, event TEXT, payload TEXT holding a\n  JSON object, created_at datetime). The payload object's keys are the\n  effect's declared field names with the post-transition row's values.\n\nEffects data:\n\n```json\n[\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"emit\",\n        \"event\": \"booking.confirmed\",\n        \"fields\": [\n          \"id\",\n          \"venue\",\n          \"startsAt\"\n        ]\n      }\n    ],\n    \"event\": \"confirm\"\n  },\n  {\n    \"effects\": [\n      {\n        \"__effect\": \"set\",\n        \"field\": \"cancelledAt\",\n        \"value\": {\n          \"__expr\": \"requestTime\"\n        }\n      }\n    ],\n    \"event\": \"cancel\"\n  }\n]\n```\n\n### Invariants (truths that must hold at ALL times)\n\n- no-overbooking: for every Venue row — count of Booking rows whose\n  venue points at it must be ≤ <row>.capacity.\n  Enforced in create/update handlers of Booking and update handlers of\n  Venue, INSIDE the request transaction: apply the mutation, re-check\n  the affected Venue row, roll back and answer 409 on violation.\n\nPinned violation outcome (exact body, row rolled back):\n409 `{\"detail\": \"Invariant violated\"}`\n\nCheck trees (data, from the specification):\n\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Protected routes require the bearer token dependency (`get_current_user`).",
            "scope": [
              "tests/spec_tasks/test_router_booking.py"
            ]
          }
        },
        "promptSha256": "1495a87ab9b225560f863bd11c07ba89fcec3e85d7ecccfeddf4e319e7025d02",
        "scope": [
          "app/routers/booking.py",
          "tests/spec_tasks/test_router_booking.py"
        ],
        "specNodeIds": [
          "api:BookingCount",
          "crud:Booking",
          "lifecycle:BookingLifecycle"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_user.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:User",
        "label": "router: User",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: User\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/user.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **User** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /users | 200 | bearer | list |\n| GET | /users/{id} | 200 | bearer | get |\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Protected routes require the bearer token dependency (`get_current_user`).",
            "scope": [
              "app/routers/user.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_user.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"router:User\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"router:User\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_router_user.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_router_user.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: User\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/user.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **User** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /users | 200 | bearer | list |\n| GET | /users/{id} | 200 | bearer | get |\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Protected routes require the bearer token dependency (`get_current_user`).",
            "scope": [
              "tests/spec_tasks/test_router_user.py"
            ]
          }
        },
        "promptSha256": "0a8e25b3acaf64b8a2119bfc69a6174b3d4231d9042db635af8cb7ca800ef670",
        "scope": [
          "app/routers/user.py",
          "tests/spec_tasks/test_router_user.py"
        ],
        "specNodeIds": [
          "crud:User"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_venue.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database"
        ],
        "id": "router:Venue",
        "label": "router: Venue",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Venue\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/venue.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/models.py, app/schemas.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **Venue** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"capacity\",\n      \"name\": \"capacity\",\n      \"type\": \"int\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\",\n      \"unique\": true\n    }\n  ],\n  \"name\": \"Venue\",\n  \"table\": \"venues\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /venues | 200 | public | list |\n| GET | /venues/{id} | 200 | public | get |\n| POST | /venues | 201 | public | create |\n| PATCH | /venues/{id} | 200 | public | update |\n| DELETE | /venues/{id} | 204 | public | delete |\n\n### Invariants (truths that must hold at ALL times)\n\n- no-overbooking: for every Venue row — count of Booking rows whose\n  venue points at it must be ≤ <row>.capacity.\n  Enforced in create/update handlers of Booking and update handlers of\n  Venue, INSIDE the request transaction: apply the mutation, re-check\n  the affected Venue row, roll back and answer 409 on violation.\n\nPinned violation outcome (exact body, row rolled back):\n409 `{\"detail\": \"Invariant violated\"}`\n\nCheck trees (data, from the specification):\n\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Every route here is public: no auth dependency.",
            "scope": [
              "app/routers/venue.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_venue.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"router:Venue\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"router:Venue\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_router_venue.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_router_venue.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: Venue\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/venue.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/models.py, app/schemas.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the API router for the **Venue** entity.\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"capacity\",\n      \"name\": \"capacity\",\n      \"type\": \"int\"\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\",\n      \"unique\": true\n    }\n  ],\n  \"name\": \"Venue\",\n  \"table\": \"venues\"\n}\n```\n\n## Routes for this router (the exact interface — no more, no less)\n| method | path | success | auth | operation |\n| --- | --- | --- | --- | --- |\n| GET | /venues | 200 | public | list |\n| GET | /venues/{id} | 200 | public | get |\n| POST | /venues | 201 | public | create |\n| PATCH | /venues/{id} | 200 | public | update |\n| DELETE | /venues/{id} | 204 | public | delete |\n\n### Invariants (truths that must hold at ALL times)\n\n- no-overbooking: for every Venue row — count of Booking rows whose\n  venue points at it must be ≤ <row>.capacity.\n  Enforced in create/update handlers of Booking and update handlers of\n  Venue, INSIDE the request transaction: apply the mutation, re-check\n  the affected Venue row, roll back and answer 409 on violation.\n\nPinned violation outcome (exact body, row rolled back):\n409 `{\"detail\": \"Invariant violated\"}`\n\nCheck trees (data, from the specification):\n\n```json\n[\n  {\n    \"count\": {\n      \"bound\": {\n        \"kind\": \"field\",\n        \"name\": \"capacity\"\n      },\n      \"entity\": \"Booking\",\n      \"op\": \"lte\",\n      \"refField\": \"venue\"\n    },\n    \"id\": \"invariant:no-overbooking\",\n    \"shape\": \"crossRowCount\"\n  }\n]\n```\n\n## Requirements\n- Register count routes BEFORE any `{id}` route of the same prefix.\n- Use the schemas from `app/schemas.py` and the models from `app/models.py`.\n- Model/base ownership is exact: routers MUST NOT define an ORM base or import\n  `Base`/`DeclarativeBase`; all mapped classes already come from\n  `app.models`. If a reference-validation helper needs a model-class type,\n  use `type[Any]` (with `Any` from `typing`) or omit that annotation.\n- SQLAlchemy import locations are exact: `func`, `select`, and `update`\n  come from `sqlalchemy`; `IntegrityError` comes from\n  `sqlalchemy.exc`; `Session` comes from `sqlalchemy.orm`. Never import\n  `DeclarativeBase` from top-level `sqlalchemy`.\n- `list`: every row ordered by created_at ascending; bare JSON array.\n- `create`: 201 + stored row; validate that ref ids reference EXISTING\n  rows — dangling refs are 404 `{\"detail\": \"Not found\"}`.\n- `update`: partial PATCH; 200 + full row.\n- `delete`: 204, empty body.\n- `count`: `{\"count\": <int>}` (total rows).\n- Unique violations → 409 `{\"detail\": \"Already exists\"}`.\n- Every route here is public: no auth dependency.",
            "scope": [
              "tests/spec_tasks/test_router_venue.py"
            ]
          }
        },
        "promptSha256": "e83fbbf1f212fcfad54bfa3c6c2b02b68d3617e3742cba811f8220690e75f52a",
        "scope": [
          "app/routers/venue.py",
          "tests/spec_tasks/test_router_venue.py"
        ],
        "specNodeIds": [
          "crud:Venue"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_auth.py"
        ],
        "dependsOn": [
          "models",
          "schemas",
          "database",
          "security"
        ],
        "id": "router:auth",
        "label": "router: auth",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: auth\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/auth.py\n- Already generated by previous tasks (read-only for you): app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the auth router (`app/routers/auth.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Auth contract\n```json\n{\n  \"identityField\": \"email\",\n  \"passwordColumn\": \"password_hash\",\n  \"principal\": \"User\",\n  \"routes\": [\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/login\",\n      \"method\": \"POST\",\n      \"operation\": \"login\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/login\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"kind\": \"token\"\n      },\n      \"status\": 200\n    },\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/register\",\n      \"method\": \"POST\",\n      \"operation\": \"register\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/register\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"email\",\n          \"name\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 201\n    },\n    {\n      \"auth\": true,\n      \"entity\": \"User\",\n      \"id\": \"GET /auth/me\",\n      \"method\": \"GET\",\n      \"operation\": \"me\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/me\",\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 200\n    }\n  ],\n  \"strategy\": \"password-jwt\"\n}\n```\n\n## Principal entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```\n\n## Requirements\n- `POST /auth/login`: body\n  `{\"email\": ..., \"password\": ...}` → 200\n  `{\"access_token\": \"<jwt>\", \"token_type\": \"bearer\"}`; wrong identity or\n  password → 401 `{\"detail\": \"Invalid credentials\"}` (identical for both —\n  no user enumeration).\n- `POST /auth/register`: body\n  = principal fields + `\"password\"` (bcrypt-hash before storage) → 201\n  with the principal row (never the hash). Duplicate identity → 409\n  `{\"detail\": \"Already exists\"}`.\n- `GET /auth/me`: bearer\n  token → 200 principal row.\n- Never serialize `password_hash`.",
            "scope": [
              "app/routers/auth.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_router_auth.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"router:auth\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"router:auth\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_router_auth.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_router_auth.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: router: auth\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/routers/auth.py\n- Already generated by previous tasks (read-only for you): app/deps.py, app/models.py, app/schemas.py, app/security.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the auth router (`app/routers/auth.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Auth contract\n```json\n{\n  \"identityField\": \"email\",\n  \"passwordColumn\": \"password_hash\",\n  \"principal\": \"User\",\n  \"routes\": [\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/login\",\n      \"method\": \"POST\",\n      \"operation\": \"login\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/login\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"kind\": \"token\"\n      },\n      \"status\": 200\n    },\n    {\n      \"auth\": false,\n      \"entity\": \"User\",\n      \"id\": \"POST /auth/register\",\n      \"method\": \"POST\",\n      \"operation\": \"register\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/register\",\n      \"request\": {\n        \"shape\": {\n          \"email\": \"email\",\n          \"name\": \"string\",\n          \"password\": \"string\"\n        }\n      },\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 201\n    },\n    {\n      \"auth\": true,\n      \"entity\": \"User\",\n      \"id\": \"GET /auth/me\",\n      \"method\": \"GET\",\n      \"operation\": \"me\",\n      \"owner\": {\n        \"kind\": \"auth\",\n        \"sourceNodeId\": \"auth:MainAuth\",\n        \"taskId\": \"router:auth\"\n      },\n      \"path\": \"/auth/me\",\n      \"response\": {\n        \"entity\": \"User\",\n        \"kind\": \"entity\"\n      },\n      \"status\": 200\n    }\n  ],\n  \"strategy\": \"password-jwt\"\n}\n```\n\n## Principal entity\n```json\n{\n  \"fields\": [\n    {\n      \"column\": \"email\",\n      \"name\": \"email\",\n      \"type\": \"email\",\n      \"unique\": true\n    },\n    {\n      \"column\": \"id\",\n      \"name\": \"id\",\n      \"type\": \"uuid\"\n    },\n    {\n      \"column\": \"name\",\n      \"name\": \"name\",\n      \"type\": \"string\"\n    }\n  ],\n  \"name\": \"User\",\n  \"passwordColumn\": \"password_hash\",\n  \"table\": \"users\"\n}\n```\n\n## Requirements\n- `POST /auth/login`: body\n  `{\"email\": ..., \"password\": ...}` → 200\n  `{\"access_token\": \"<jwt>\", \"token_type\": \"bearer\"}`; wrong identity or\n  password → 401 `{\"detail\": \"Invalid credentials\"}` (identical for both —\n  no user enumeration).\n- `POST /auth/register`: body\n  = principal fields + `\"password\"` (bcrypt-hash before storage) → 201\n  with the principal row (never the hash). Duplicate identity → 409\n  `{\"detail\": \"Already exists\"}`.\n- `GET /auth/me`: bearer\n  token → 200 principal row.\n- Never serialize `password_hash`.",
            "scope": [
              "tests/spec_tasks/test_router_auth.py"
            ]
          }
        },
        "promptSha256": "66b904e35d65822167e0ce5f6580baa9b27db3fda4f1ca4b322ab2df8e281a49",
        "scope": [
          "app/routers/auth.py",
          "tests/spec_tasks/test_router_auth.py"
        ],
        "specNodeIds": [
          "auth:MainAuth"
        ]
      },
      {
        "acceptanceCommands": [
          "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_app.py"
        ],
        "dependsOn": [
          "router:Booking",
          "router:User",
          "router:Venue",
          "router:auth",
          "database"
        ],
        "id": "app",
        "label": "application wiring",
        "loop": {
          "implementation": {
            "instruction": "You are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: application wiring\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/main.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/routers/auth.py, app/routers/booking.py, app/routers/user.py, app/routers/venue.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the application factory (`app/main.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Application\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```\n\n## Complete route interface (register every one, nothing else)\n- GET /bookings\n- GET /bookings/{id}\n- POST /bookings\n- DELETE /bookings/{id}\n- GET /users\n- GET /users/{id}\n- GET /venues\n- GET /venues/{id}\n- POST /venues\n- PATCH /venues/{id}\n- DELETE /venues/{id}\n- GET /bookings/count\n- POST /bookings/{id}/confirm\n- POST /bookings/{id}/cancel\n- POST /auth/login\n- POST /auth/register\n- GET /auth/me\n\n## Requirements\n- `app/main.py` MUST export\n  `create_app(database_url: str | None = None) -> FastAPI` AND a\n  module-level `app = create_app()`.\n- Title `Booking API`, version `0.1.0`.\n- Include every router produced by previous tasks; count routes must be\n  reachable. Import `ROUTERS` from the compiler-owned\n  `app.router_registry` and include each entry exactly once in tuple order.\n  Do not import or register routers by any other path.\n- Call `create_engine_from_url(database_url)` and\n  `create_session_factory(engine)` once per application. Store the engine and\n  factory on `app.state`, override `get_db` with\n  `session_dependency(factory)`, and create tables on startup against that\n  engine. Dispose that engine on shutdown. This is what makes separate\n  `create_app(database_url=...)` calls isolated.\n- Construct deterministic in-memory cache, messaging, and blob adapters by\n  default and expose them as `app.state.cache`, `app.state.messaging`,\n  and `app.state.blob` when their corresponding contracts exist. Provider\n  adapters remain available for deployment, but tests require no external services.\n- Do not add extra routes: the conformance suite asserts strict OpenAPI\n  equality (FastAPI's automatic /openapi.json and /docs are fine).",
            "scope": [
              "app/main.py"
            ]
          },
          "maxRounds": 3,
          "reviewer": {
            "commands": [
              "uv run --no-project --python '3.13' --with 'bcrypt==5.0.0' --with 'email-validator==2.2.0' --with 'fastapi==0.141.1' --with 'httpx==0.28.1' --with 'pydantic==2.13.5' --with 'pydantic-settings==2.15.0' --with 'pyjwt==2.13.0' --with 'pytest==9.1.1' --with 'sqlalchemy==2.0.52' --with 'uvicorn==0.52.4' python -B -m pytest -p no:cacheprovider -q tests/spec_tasks/test_app.py"
            ],
            "instruction": "You are the read-only code reviewer for generation node \"app\". Verify both implementation and tests against the exact frozen specification. Look for missing behavior, extra public API/routes, ABI drift, invalid imports, tests that merely mirror the implementation, and uncovered constraints. Use test failures and direct code inspection. If rejecting, give actionable changes for both source and tests where applicable."
          },
          "schemaVersion": "spec-agent-task-loop/0.1",
          "tests": {
            "instruction": "You are the unit-test author for generation node \"app\".\nYou and the implementation agent receive the same frozen specification progress. Read the dependency files and the complete node contract below, then create focused executable pytest tests in tests/spec_tasks/test_app.py. Test declared behavior, exact public ABI, forbidden extras, and relevant failure cases. Do not weaken, delete, or rewrite tests to accommodate an implementation. Do not edit source files.\n\nYour working directory already mirrors the node's task workspace. A compiler-owned scaffold for your file already exists at exactly tests/spec_tasks/test_app.py relative to your current directory — edit that file in place and never create tests anywhere else; that exact path is your only owned file. The \"Your scope\" list inside the contract below belongs to the implementation agent and is NOT your scope.\n\nFrozen implementation contract:\nYou are executing ONE TASK of a larger, compiler-planned generation.\n\n# Task: application wiring\n\nThe specification compiler derived this task from the user's\nspecification. Files from previous tasks already exist in this workspace\nand are part of the same application: READ them for context, match their\nconventions, and do NOT modify anything outside your scope.\n\n- Your scope (create/modify ONLY these): app/main.py\n- Already generated by previous tasks (read-only for you): app/config.py, app/database.py, app/routers/auth.py, app/routers/booking.py, app/routers/user.py, app/routers/venue.py\n- Put every scratch file, downloaded package, temporary virtualenv, and tool\n  cache outside the repository (use `/tmp`). Never create `.pkg-tmp`, a\n  temporary dependency directory, or any other unowned path in the workspace.\n- The available shell is intentionally narrow. Do not invoke `pip`, `venv`,\n  `git`, shell redirection, pipes, `cd`, or chained shell commands. If a\n  focused runtime probe is truly necessary, use one command beginning with\n  `uv run --no-project --with <pinned-package> ...`; otherwise reason from\n  the pinned contract and write only the owned files.\n\n\n## Target and package engineering guidance\n\n### @spec/fastapi · python-fastapi-baseline\n- Use Python 3.13 type hints on public functions and concrete return types; avoid Any except at serialization boundaries.\n- Keep asynchronous I/O non-blocking and move unavoidable blocking SDK calls to asyncio.to_thread.\n- Create network clients and pools in FastAPI lifespan, inject them into services, and close them in reverse order.\n- Use typed settings for configuration, explicit timeouts, bounded retries, and structured logging without secrets or payload bodies.\n- Keep provider adapters behind small protocols and make every behavior independently testable with deterministic in-memory adapters.\n- Do not catch broad exceptions unless translating them into a contract-defined typed error while preserving the original cause.\n\nThis guidance is subordinate to the compiler-derived behavioral contract.\nIf guidance appears to conflict with the blueprint, implement the blueprint.\n\n\nImplement the application factory (`app/main.py`).\n\n## Global invariants (hold for every task)\n- Entity JSON keys are the field names EXACTLY as declared (camelCase stays camelCase).\n- ref fields serialize as the referenced row's id string.\n- Implicit columns (created_at on every entity, password_hash on the auth\n  principal) are NEVER serialized; created_at only orders lists (ascending).\n- list returns EVERY row of the entity (including the requesting principal\n  when applicable) as a bare JSON array, ordered by created_at ascending.\n- Fields with a declared default are omittable in create bodies; the\n  default applies when omitted; optional-without-default stores null.\n- Server generates uuid4 ids; id in request bodies is ignored.\n- Error bodies (exact): 401 {\"detail\": \"Not authenticated\"} ·\n  404 {\"detail\": \"Not found\"} (unknown id AND dangling refs) ·\n  409 {\"detail\": \"Already exists\"} (unique violations) · 422 = FastAPI default.\n- Path parameters are named exactly `id`.\n- Do not add any route, file or path beyond your scope — the conformance\n  suite asserts STRICT OpenAPI equality.\n- SQLite-compatible SQL only (tests run on SQLite).\n\n## Application\n```json\n{\n  \"name\": \"BookingAPI\",\n  \"port\": 8000,\n  \"prefix\": \"\",\n  \"title\": \"Booking API\",\n  \"version\": \"0.1.0\"\n}\n```\n\n## Complete route interface (register every one, nothing else)\n- GET /bookings\n- GET /bookings/{id}\n- POST /bookings\n- DELETE /bookings/{id}\n- GET /users\n- GET /users/{id}\n- GET /venues\n- GET /venues/{id}\n- POST /venues\n- PATCH /venues/{id}\n- DELETE /venues/{id}\n- GET /bookings/count\n- POST /bookings/{id}/confirm\n- POST /bookings/{id}/cancel\n- POST /auth/login\n- POST /auth/register\n- GET /auth/me\n\n## Requirements\n- `app/main.py` MUST export\n  `create_app(database_url: str | None = None) -> FastAPI` AND a\n  module-level `app = create_app()`.\n- Title `Booking API`, version `0.1.0`.\n- Include every router produced by previous tasks; count routes must be\n  reachable. Import `ROUTERS` from the compiler-owned\n  `app.router_registry` and include each entry exactly once in tuple order.\n  Do not import or register routers by any other path.\n- Call `create_engine_from_url(database_url)` and\n  `create_session_factory(engine)` once per application. Store the engine and\n  factory on `app.state`, override `get_db` with\n  `session_dependency(factory)`, and create tables on startup against that\n  engine. Dispose that engine on shutdown. This is what makes separate\n  `create_app(database_url=...)` calls isolated.\n- Construct deterministic in-memory cache, messaging, and blob adapters by\n  default and expose them as `app.state.cache`, `app.state.messaging`,\n  and `app.state.blob` when their corresponding contracts exist. Provider\n  adapters remain available for deployment, but tests require no external services.\n- Do not add extra routes: the conformance suite asserts strict OpenAPI\n  equality (FastAPI's automatic /openapi.json and /docs are fine).",
            "scope": [
              "tests/spec_tasks/test_app.py"
            ]
          }
        },
        "promptSha256": "f300d975c8a7bb4563ba98b64654baa1638495defa7c88624fb57d36805a34ab",
        "scope": [
          "app/main.py",
          "tests/spec_tasks/test_app.py"
        ],
        "specNodeIds": [
          "app:BookingAPI",
          "fastapi:Server"
        ]
      }
    ]
  },
  "verification": {
    "check": [
      {
        "command": ".venv/bin/python -c \"from app.main import app, create_app; assert app.title\"",
        "name": "import",
        "timeoutMs": 60000
      },
      {
        "command": ".venv/bin/python -m pytest conformance -q",
        "name": "conformance",
        "timeoutMs": 300000
      }
    ],
    "setup": [
      {
        "command": "uv venv .venv --clear --quiet --python 3.13",
        "name": "venv",
        "timeoutMs": 180000
      },
      {
        "command": "uv pip install --quiet -e '.[dev]'",
        "name": "install",
        "timeoutMs": 600000
      }
    ]
  }
}
